{
  "note": "Accepted ownership gaps: tracked code files sitting in a directory the ownership map enumerates file-by-file, without a pattern of their own. The router still reviews them by directory inheritance, but no role has named them. This is the BASELINE the generator ratchets against — a NEW gap fails `npm test` (docs/org/gen-ownership-map.test.mjs), which gates deploy.yml — so a new gap fails the DEPLOY, not just this script. Shrink it by naming files in docs/role-responsibilities.md; re-baseline only on purpose.",
  "generatedBy": "docs/org/gen-ownership-map.mjs --update-gaps",
  "count": 0,
  "accepted": []
}
