# tasks/todo.md — scratch

No active plan. The last one (BIN-593 — `watchedAt` is user-authored data) shipped in
`8d8734c`; its full record lives in that commit's message, the in-code comments at
`buildStatusUpdate`/`addItem`, and `.claude/state/simplify-done.marker`.

Open follow-ups it filed: BIN-595, BIN-596, BIN-597 (needs Malin's product call on
"Rensa datum"), BIN-598, BIN-599.

This file is disposable scratch (see .claude/rules/code-style.md) but is TRACKED and is
the repo's `planTodoPath`, so replace its contents rather than deleting it.
