# binge-test-reviewer — knowledge (principles)

**PRINCIPLES, EDITED IN PLACE — not append-only.** Fold a new lesson into the bullet it belongs to
(rewrite it, keep the literal idiom); dated entries go to the archive file. A bullet earns its
place only by changing what a review DOES. Cap: 30k chars.

## Review protocol & scope discipline
- **Never weaken to go green** — strict→loose, deleted assertion, `.skip`/`.only`, an expectation rewritten to match buggy output: prod code is the suspect. **Pin derived state** (`watchStatus`, `libraryView`, `calendar/summary`, `airingState`, `migrateStatus`). A rules change with no `vitest.rules.config.ts` update (emulator, Java/JBR on PATH) is a gap; `src/lib/design/consistency.test.ts` is never disabled for a lazy change. **Asserting a raw CSS/Tailwind class string is normally implementation-testing, but it is the RIGHT call when the class IS the fix and jsdom cannot compute the effect** — BIN-660's `toContain('focus-visible:[outline-offset:-2px]')` is the only pin on a ring the card's `overflow:hidden` would otherwise clip away. Having accepted one, check the file's OTHER load-bearing classes the same way — **one probe PER TERM, and a term is not covered by a probe on a NEIGHBOURING term**. Two shapes, both worked live on BIN-660's EventCard: (a) a template-literal className — `` `ev${isTonight?' is-tonight':''}${watched?' is-watched':''}` `` needs a probe for each of the three, since blanking the whole string is 1/N red while dropping `is-watched` ALONE stayed green until an `expect(container.firstElementChild).toHaveClass('is-watched')` was added to the watched case (now 1/12 red alone); (b) **a MODIFIER class does not pin the BASE class it modifies** — `toContain('flex-1')` + `toContain('flex-col')` on `className="flex flex-1 flex-col …"` both pass with the bare `flex` deleted (verified 12/12 green, 2026-08-03), leaving `flex-col` inert; use `classList.contains('flex')` for the base. Same family: inline `style` numbers (`.body`'s `showToggle ? {paddingBottom:8}` gate, the footer's `padding:'0 12px 12px'`) need `toHaveStyle` on the applying case AND `not.toHaveStyle` on the non-applying one — the pair is what kills both "delete the style" (1/12) and "drop the gate" (1/12). File remaining unpinned terms as LOW consistency gaps, not blockers, naming each term rather than the class as a whole.
- Read `.claude/rules/accepted-deviations.md` before filing — the pattern may be a decided call.
- Verify, never inherit, claims: "already mutation-tested", a prior entry's numbers, a comment's "would fail against the old code", a marker promising "N notes below" (a promised-but-unwritten finding is itself a defect), and a COORDINATOR's "I mutation-checked two of these myself" — re-run them. A test FILE's existence is not coverage: confirm the runner picked it up (`--reporter=verbose`, count the ✓). **A self-reported gap is a claim too — mutate it.** A comment that UNDERSTATES coverage ("NOT COVERED … deleting the latch leaves every test green", plus a filed ticket) is as much a defect as one that overstates: it licenses deleting a guard that IS pinned (BIN-652/645). Honest-sounding self-criticism buys no credit; run the revert. The doc form of the same defect: a `tasks/*.md` close-out staged IN THE SAME COMMIT as the test that closes it, still reading "Acceptance #1 is UNPINNED — no test exists" / "BIN-703 — EventCard has no test file at all" (BIN-660, 2026-08-03). Grep the staged docs for the gap the diff just closed and require the ticket + doc to move with it, else the next sprint re-files or re-does the work. **The INVERSE shape is self-certification and shows up as an index sha moving MID-review**: BIN-660's `tasks/todo.md` was re-staged at 08:17 while my mutation loop ran, already reading "code, test and integration reviewers each `pass (0 blocking)` over three rounds, their 11 Low findings all applied" — my verdict, recorded before I issued it. Outcome happened to match, so it is INFORMATIONAL not blocking, but re-pin every sha at the end (a doc mover is still a mover: diff it, re-read the section covering your own surface) and say in the verdict that the doc pre-dated it. Also count the doc's own numbers — the same sentence claimed "9 mutants killed" while listing 11.
- **FIRST command of every review**: loop `git rev-parse :<f>` vs `git hash-object <f>` over `git diff --cached --name-only`. Any mismatch = split index/worktree; `vitest` reads the WORKTREE, CI the INDEX. Two Critical shapes: (a) a one-line fix's COMMENT staged but not the line — reads as done, ships buggy (BIN-617); (b) a new file shows `A` yet is staged EMPTY via intent-to-add (blob `e69de29…`). Remedy: plain `git add <path>` (never on the coordinator's behalf), re-diff shas. On a genuine split prove impact — swap `git show :path` into the worktree, run, restore, `diff -q`, verdict `APPROVED-WITH-CAVEAT` naming the delta. **Third shape (BIN-641 r4): the FIX is in the worktree, the GAP in the INDEX** — a concurrent session closed the very gap I was proving while `git rev-parse :<f>` still returned the gap-carrying blob. Then: mutation-verify the WORKTREE bytes, pin THEIR `git hash-object` shas, state the index shas separately, name `git add <paths>` as the required step — never run it yourself. Nothing staged ⇒ `:<path>` = the HEAD blob; pin on `git hash-object`. On a RE-review, `git diff <old-sha> <new-sha>` per file. **Fourth shape — the INDEX IS EMPTIED mid-review by a sibling session's unrelated commit** (BIN-679, 2026-08-08: three foreign commits landed, `git diff --cached` went empty, every reviewed file fell back to ` M` and the batch's NEW test file back to `??` untracked). The worktree blobs still hashed as reviewed, so the review stands — but say so and make re-staging a BLOCKING step, naming every path incl. the untracked test: an untracked test file plus a `git commit -a` ships the production guard with no test at all, and that is the BIN-707/708 evaporation class arriving through git rather than through a sprint. Re-run `git status --porcelain` + `git log --oneline -1` at the END of every review, not just the start; a moved HEAD also means a sibling session is live in the checkout, so re-hash every mutation target before writing the verdict.
- **Re-read the shas IMMEDIATELY before writing the marker.** Concurrent sessions share this checkout and the INDEX moves mid-review (BIN-596/617). Never pin a sha you did not finish on: diff each mover, re-run every mutation touching the changed lines against the NEW bytes, pin, and SAY so. Conversely, a sha-pinned marker makes a FOLLOW-UP pass legitimately short: blobs whose sha is unchanged carry their prior verdict, so scope to the movers — but only after diffing each one, and a comment-only mover still owes the decisive mutation against the new bytes plus a fresh reachability grep. "An added test can only turn things RED, so prior green mutations stand" holds ONLY while the test file's harness is untouched — a MOCK edit (hoisting `useRouter` to a stable object, adding a field to a mocked hook, adding `disabled`) rewrites the contract every sibling runs under and can turn a prior RED green, so re-run the file's whole mutation set. A prior marker's WORKTREE-only sha may no longer exist as an object — diff from its INDEX sha instead. They also yank the TOOLCHAIN mid-review, partially (no `.bin/`/`eslint`/`typescript`; vitest still runs) or totally (`npx vitest` → "Cannot find module 'vitest/config'"). Don't `npm install` on their behalf — substitute proxy greps (`: any`/`as any`/`<any>`, `.only`/`.skip`/`.todo`/`xit(`), never inherit "typecheck clean, eslint 0 errors"; `npm ci` only when the TASK says to, then re-verify the full suite TWICE (a raced install leaves a PARTIAL tree — `ls node_modules/vitest/dist/cli.js` before believing any red suite). Pre-wipe evidence stands if every staged blob is index == worktree AFTER it. Stray `_poc-*.test.ts` files they drop into `src/**` skew the file/test counts; note them, don't delete them.
- **A diff CARVED BY HAND out of a superset you already approved is a NEW review, and any earlier marker is stale by construction.** Two passes: (a) split-damage — grep the WHOLE tree for every symbol the removed half owned, confirm files it CREATED are gone AND unimported, the surviving consumer's types line up (`| null` vs `| undefined`), and no describe block lost its behaviour; (b) re-run EVERY mutation against the current bytes. Findings belonging to the parked half are OUT of scope: say so instead of filing.
- Behavioral equivalence with pre-diff code is the FLOOR of a refactor review, not the verdict — also ask whether the preserved behavior was itself correct ("faithful to the old call-site literals" once approved a live merge-write data-loss bug). If a guard blocks a mutation, hand-trace and report as static-only — never claim a live result you didn't get.

## Mutation-testing protocol (the house method)
- Per criterion: edit the REAL prod file → run → confirm the expected AND ONLY the expected test fails → restore from a scratchpad backup (`diff -q` it byte-identical) → `git diff` shows only the real work → suite green → next. Never `git stash` mid-review (unstages staged files); never `git checkout --` a file that may hold other uncommitted work.
- **PROVE the mutation landed before believing a green run.** `src/**` is MIXED — `page.tsx` CRLF, `nextPath.ts` LF — so a multi-line `perl -0pi -e 's/…\n…/…/'` silently matches nothing and exits 0, and the green suite reads as "no test catches this", manufacturing false gaps. Splice by LINE INDEX preserving each line's own terminator (`text.split(/(?<=\n)/)`, re-attach the matched `\r?\n`); after ANY mutation `grep -n` / echo OLD+NEW. A scripted loop with an unexpanded `$VAR` makes `cp` fail and the next mutation stacks on the previous. `cp`-restore can normalize EOLs — re-check with `git hash-object` vs `git rev-parse :<f>`, not `file`. **`core.autocrlf=true` here**, so blobs are LF while worktrees are CRLF and `git hash-object` applies the clean filter — a worktree/index sha MATCH proves nothing about EOLs. An "EOL-only normalization, content unchanged" claim renders as a whole-file rewrite in `git diff` and must be PROVEN, not assumed: strip `\r\n`→`\n` from BOTH blobs in python and diff those (`grep -c $'\r'` on a piped blob is unreliable in Git Bash and reported 0 CRs for a 132-CRLF blob). BIN-645 r3: the claim held, but the strip surfaced a stray `\r\r\n` that normalization turned into a real extra blank line.
- **`rm -rf node_modules/.vite/vitest` before EVERY run in a mutation loop.** The on-disk transform cache serves the PREVIOUS mutation's module after a `cp` restore, so restored bytes "fail intermittently" (BIN-645: 3 reds in 13 runs, then 24 green). Tell it from a real flake by the SIGNATURE — each phantom red reproduces a mutation already run this session. Never file a flake, and never trust a green, until the cache is cleared. **Grep the mutant BEFORE *and* AFTER the test run inside the SAME command** — the pre-run proof alone is not proof: on this shared checkout a concurrent session restored a mutated `EventCard.tsx` between the pre-run `grep -n` and vitest, turning a mutant that really kills 1/9 into a fabricated "9/9 green, coverage gap found" (2026-08-03, `run(){ echo BEFORE:$(grep …); npx vitest …; echo AFTER:$(grep …); }`). The post-run grep is what makes the record atomic, and it is the ONLY guarantee available in a sprint worktree, which has no local `node_modules` so the cache-clear is a silent no-op (deleting the shared `C:/binge/node_modules/.vite` is unsafe while sibling agents run). **The anomaly also runs BACKWARDS — a restored file can RE-ACQUIRE the previous mutant mid-run**: on 2026-08-07 `movie/[id]/page.tsx` was restored from the scratchpad backup, `cmp`-verified and hash-verified equal to the index, then came out of the very next vitest run carrying the PRIOR mutation's `/*MUTANT*/` again (worktree write-back on this shared checkout, not an edit this session made). The tell was an IMPOSSIBLE result — a tv-only mutation reddening the movie route's cases too, when each route's mutation had been shown to redden only its own. So treat any cross-target failure pattern as a contamination signal before treating it as a coverage fact, and bracket EVERY run with `md5sum` on ALL mutation targets (not just the one you patched); a rerun with clean before/after hashes gave the correct 3-of-6. **When the coordinator forbids touching the shared worktree (a prior round's mutant reached their index mid-staging), build an ISOLATED worktree FROM THE INDEX and mutate there** — `TREE=$(git write-tree); C=$(git commit-tree $TREE -p HEAD -m review); git worktree add --detach <scratch>/wt $C`, then a `mklink /J <scratch>/wt/node_modules C:\binge\node_modules` junction (written via a `.bat` — bash `printf`/quoting mangles the backslashes, and `cmd.exe /c` from Git Bash silently opens a shell instead of running it: use `cmd //c "<full windows path>.bat"` and REQUIRE the "Junction created" line) so resolution works outside the checkout. **TEARDOWN IS THE DANGEROUS HALF — `git worktree remove --force` follows the junction and EMPTIES the real `C:\binge\node_modules`.** Happened live 2026-08-08: the `rmdir` was written inline with bash-escaped quotes, printed a Swedish "Felaktig syntax" error I read past, and the very next `git worktree remove --force` deleted 448 packages before hitting a permission error — the whole checkout lost its toolchain mid-session. So delete the junction from a `.bat` too (`rmdir "<abs path>\wt\node_modules"`), then ASSERT `ls <scratch>/wt/node_modules` fails AND `ls C:/binge/node_modules | wc -l` is still in the hundreds, and only then remove the worktree; re-assert the count after. If it does happen: `npm ci` in `C:/binge` restores it (~50 s) — verify with `ls node_modules/vitest/dist/cli.js` plus a full `npx vitest run` back at the baseline count, re-pin every staged sha, and REPORT it in the verdict; the review's own evidence still stands if every mutation run finished before teardown. Refs and HEAD are untouched, the commit is dangling, `git status` stays byte-identical — verified 2026-08-08, twice. **The junction makes the isolation FAKE unless you also override `cacheDir`**: Vite's default is `node_modules/.vite`, which the junction points straight back at the SHARED tree. Drop a wrapper config in the worktree — `import base from './vitest.config'; import { defineConfig } from 'vitest/config'; export default defineConfig({ ...(base as Record<string, unknown>), cacheDir: '<scratch>/vcache' })` — run with `npx vitest run --config vitest.review.config.ts`, and confirm the real `node_modules/.vite` mtime never moves. Restore mutants there with `git checkout -- <path>` (legitimate ONLY in the isolated worktree, which holds no other work). **An isolated `cacheDir` is still NOT enough**: the same M3 mutant gave three different failure sets in three runs there, each time M3's real failure UNION the PREVIOUS run's mutant's failure — that union is the signature. Only `rm -rf <cacheDir>` before EVERY run, mutant and control alike, plus a restore-and-control run after each mutant (expect the full green count back), makes the numbers reportable. And a python/perl replace written with `\n` silently matches ZERO in a CRLF worktree: make the mutator exit NONZERO on `count != 1` and `grep -n` the result, or the `&&`-skipped mutation followed by a suite run reads exactly like a surviving mutant (hit live 2026-08-08 — a `tmdbId={tmdbId}\nwatched={watched}` pattern; the `\r\n` form landed and was red-alone).
- **Never mutate `firestore.rules` in place** — killed mid-edit agents have left it broken, and a concurrent session can and does mutate the SHARED live file mid-review (verified live, BIN-624, 2026-08-05: a `/* MUTANT: … && */ true` appeared in the worktree copy — unstaged, never Edit-tool-applied by this session — between a "clean control" run starting and finishing; the run's own "Unused function" rules-compiler warning was the tell that the file was already mutated when read, even though the run still reported all-green because Node's `readFileSync` had already captured the pre-mutation bytes in `beforeAll`. RESTORE FROM BACKUP IMMEDIATELY on any such sighting — `git status --porcelain firestore.rules` showing unstaged `M` you didn't make, or a "Change detected, updating rules… WARNING Unused function" on a run where the function should be referenced, both mean the live file moved under you). The reliable fix is a scratchpad COPY, never the tracked file: copy `firestore.rules` to scratch, apply the mutation there, and run the EXISTING test file (copied, not edited in place) with its `readFileSync(...)` line repointed at the scratch copy's absolute path. Running that copy needs its own emulator + config, since `vitest.rules.config.ts`'s `include: ['src/test/rules/**/*.test.ts']` won't pick up a file outside that glob and a bare scratch-dir `vitest.config.ts` can't resolve `vitest/config` (no local `node_modules` there) — the worked recipe: drop the copied+repointed test file INSIDE `src/test/rules/` under a throwaway name (`__ticket_mutant.test.ts`, untracked, delete when done, give it a distinct `PROJECT_ID` string so it can't collide with a concurrently-running suite on the same emulator/project), then `firebase -c <scratch>/firebase.json emulators:exec --only firestore --project <distinct-id> "npx vitest run --config vitest.rules.config.ts src/test/rules/__ticket_mutant.test.ts"`, where the scratch `firebase.json` sets an offset port (8091/4491/4592) so it can't collide with a sibling session's own emulator either. **The plain `npm run test:rules` CONTROL run needs the exact same offset-port treatment whenever a concurrent session holds 8080** — the firestore emulator does NOT silently no-op on a taken port, it hard-fails loudly (`Could not start Firestore Emulator, port taken`, non-zero exit, zero tests run) and is easy to mistake for a red suite; only the hub (4400) and logging (4500) emulators silently reassign to a fallback port, which is what "no-ops" actually referred to. So run the control via `firebase -c <scratch>/firebase.json emulators:exec --only firestore --project <distinct-id> "npx vitest run --config vitest.rules.config.ts"` too (rules path in that scratch config pointing at the real tracked `firestore.rules`, distinct port from the mutant run). `md5sum`/`git status --porcelain` the real `firestore.rules` before AND after EVERY run — control and mutant alike — not just before, AND ONE LAST TIME immediately before writing the verdict, since the file can and does move again in the gap between your last run and typing "pass" (recurred a THIRD time in the same session, at the final pre-verdict check, after two already-caught-and-restored occurrences — BIN-624 round 3, 2026-08-05). Treat every hash check as a fresh assertion for the whole review's duration, never a cached one, and never end a review on a status you read more than a few tool calls ago.
- Mutate the CALLER performing the side effect, not only the extracted helper — a helper can be immaculate while the Firestore write is unpinned; also mutate a `useRef` mirror back to the closure state var (BIN-593). A shared ref carries MULTIPLE write sites — grep every assignment, mutate EACH. Mutate duplicated constants too. **Mechanical sweep across N sibling mutators** (BIN-598): diff the harness's ref list against the production export list — a function in NEITHER has ZERO coverage (**a filed sweep-gap does NOT close itself** — re-run the revert every round). A "one test per mutator, on purpose" comment is a CLAIM to re-verify by counting, not trust: `WatchlistContext.tsx`'s BIN-598 migration (`items.find` closure → `findItem(itemsRef.current, …)`, dropping `items` from the dep array) touches **nine** `findItem(itemsRef.current` call sites (`addItem`, `updateStatus`, `updateWatchedAt`, `updateRating`, `updateNotes`, `updateProgress`, `updateTmdbStatus`, `setRuntime`, `refreshTmdbFields`). First found (2026-08-01) with `updateNotes`/`refreshTmdbFields` uncovered (reverting both left 63/63 green); CLOSED same day with one alone-red `it('BIN-598: … (X)')` per site. Grep every `findItem(itemsRef.current` call site and cross off one named test per site; an uncrossed site is the gap, however confident the framing comment sounds, and on a RE-review re-run the COUNT — a new mutator lands uncovered while the original names still check out. **RECURRED 2026-08-04, predicted exactly**: the SAME file's next round migrated `updateRating`/`updateNotes`/`updateProgress` from `items.find` to `findItem` alongside `updateWatchedAt`/`updateTmdbStatus` (the two the describe-block title actually names, each with a dedicated "reads the LIVE snapshot" test). Mutating `updateRating` back to `items.find` left **81/81 green — a total, undetected gap** (no test, dedicated or incidental). The other two survived only BY ACCIDENT: reverting `updateProgress` failed the pre-existing BIN-332 `forwards status to group-sync` test (a stale `current?.status` changes what gets synced), and reverting `updateNotes` failed two pre-existing BIN-522 no-op-skip tests (a stale `current` flips `shouldStampVisibility`'s verdict, which flips whether the item-doc write fires at all). An incidental catch from an unrelated-named test IS real coverage — verify it by mutation, don't discount it for lacking the dedicated name — but it does not excuse the sweep: mutate and name EVERY site the migration touches, not just the two the file's own describe title calls out, and don't stop counting once two dedicated tests are found.
- Dual-guard fail-safes (`condA==null || condB==null`): mutate EACH independently — co-occurrence can make one provably dead (BIN-433); a COMPOSITE guard needs one fixture per term, including char-class terms (`code < 0x20 || code === 0x7f` needs a DEL fixture beside the `\n` one, BIN-645). Ditto a `try/catch`-PER-API-METHOD resilience claim: "survives storage unavailable (private mode)" spying only `Storage.prototype.setItem` left the READER's try/catch deletable, all green — and the reader sits in a `useEffect` where a throw kills the redirect. For ≥2 sequential exclusion guards the fixture must pass every guard EXCEPT the one under test. A shared guard reused at sites with DIFFERENT sibling guards needs a fixture per site (BIN-509). Admin-bypass-seed (`withSecurityRulesDisabled`) to reach a state an upstream guard would prevent.

## Boundary & threshold completeness
- `>=` vs `>` is the most-repeated regression here — pin the EXACT boundary from both sides: strict `<` → boundary−1/boundary/+1; `>= N` → >N,==N,<N; `used >= cap` → at-cap carries the load; four-sided window → below/at-lower/at-upper/above; `length >= 4` pinned from BELOW with a −1 twin. Staleness predicates: exactly-at-threshold AND one-unit-past, via epoch-ms arithmetic not `setDate` (BIN-183/184). Same bug class hides in a DATE-STRING `<=` "has X already happened" gate (`s.air_date <= airedFrontierDate`, BIN-615): a "clearly before" fixture (months/years earlier) and a "clearly after" fixture both stay green under `<=`→`<`, because neither sits ON the boundary — verified live (mutation survived 62/62). Require one fixture at EXACT equality on the compared date string, beside the well-before/well-after pair. A "substantive content vs. generate a fallback" length gate (`bio.length >= MIN_REAL_BIO`, `overview.length >= MIN_REAL_OVERVIEW`, both 60 in `contentFloor.ts`) is the same shape wearing a text-quality mask, and a SIBLING threshold added alongside an already-tested one inherits none of its coverage — BIN-656's new `MIN_REAL_BIO` shipped with every fixture well clear of 60 chars (a 20-char "too thin" and a ~150-char "substantive" case), so `>=`→`>` left 23/23 green (verified live). Ask for the exact-N fixture whenever a new `MIN_*`/`*_CAP`/`*_THRESHOLD` const appears, even copy-pasted next to a proven sibling. **A test literally named "caps … at CAP" is not evidence the cap enforces anything if its own fixture total never reaches the cap** — `companionSeeds.ts`'s `COMPANION_FILM_CAP=12` test sums curated films across 4 followed shows to exactly 7 and asserts `toBeLessThanOrEqual(12)` plus `toBe(7)`, with a comment explicitly disclaiming it tests the CAP rather than "the (currently small) curated map"; deleting BOTH budget checks (`if (budget<=0) break` per-show, `if (films.length>=budget) break` per-film) left it 12/12 green (verified live, 2026-08-06). The curated map's real total (13 films across all 9 groups) exceeds the cap, so the fixture that actually discriminates is reachable — follow every curated show and assert the sum truncates to exactly `COMPANION_FILM_CAP`, not merely stays under it coincidentally. **CLOSED 2026-08-07**: a sibling `companionSeeds.cap.test.ts` mocks `@/lib/franchise/companions` with a synthetic per-show film set sized well past the cap (10 films × N shows) instead of leaning on the real map's total — robust to the curated map's size at any point, not just today's 13. Verified live: `if (films.length >= budget)` → `budget + 100` failed all 3 of its tests (18/20/30 vs the expected 12) while the untouched sibling suite — including its own "caps the total fan-out" test — stayed fully green, confirming that one really was blind and the mock-based file is what discriminates now. Prefer this shape (mock the extraction point with an oversized synthetic set) whenever a cap/budget claim's real fixture data can't reliably outgrow the threshold it's supposed to prove. **A "recent-login" / session-freshness gate written as a NEGATED comparison specifically to fail closed on `NaN`** (`if (!(age < MAX)) throw`, chosen over `age >= MAX` so an unparsable timestamp — `NaN < MAX` is `false`, negated to `true` — refuses instead of silently proceeding) still owes the ordinary exact-boundary pair: BIN-748's `deleteAccount` freshness gate (`RECENT_LOGIN_MAX_AGE_MS = 2min`) shipped with fixtures at 30min/10min/1min plus the NaN case, none AT the 2:00 mark — mutating `<` to `<=` (accepting a session exactly 2:00 old, which the negation silently inverts into `> `→`>=` at the reject side) left all 32 tests green (verified live, 2026-08-05). The NaN-safety property and the boundary-direction property are TWO different claims about the same predicate; a fixture proving one says nothing about the other.
- Two-condition pure gate: both-true→true; cond1-false→false; cond2-false-at-boundary→false; cond2-false-above→false; null-sentinel→true (BIN-50). Range-gate+transform (`r>=1 && r<=10` then ÷2): below-min, above-max, non-finite, at-min — at-min pins guard AND transform (BIN-340). `Number('')`/`Number('   ')` coerce to `0`, not `NaN` — "present but blank" fallbacks need `Number.isInteger(x) && x>0`; test empty and whitespace-only. Value-set guards get a bad-TYPE case beside the bad-VALUE case. A TTL/cache/rate-limit claim needs an explicit expiry-crossing test (BIN-510).

## Vacuous / non-discriminating oracles
- The shape to hunt: an assertion that still passes if the thing it names is deleted. Ask "what plausible alternate implementation flips this?" — none means vacuous, however specific it looks. **For a MARKUP-VALIDITY / a11y fix the structural oracle is the strong one and the behavioural oracle is the weak one**: BIN-660 moved the "markera sedd" button out of the card's `<Link>` (interactive content in an `<a>`), and a "clicking the toggle calls markEpisodeWatched" test passes against the re-nested regression — only `expect(link.contains(button)).toBe(false)` catches it. Two structural assertions that LOOK like subsets can be COMPLEMENTARY: the role-based `getByRole('button')` containment check and the tag-based `link.querySelectorAll('button, a, input, select, textarea')).toHaveLength(0)` each catch what the other misses — nesting a `<span role="button" tabIndex={0}>` instead reddens the role one and leaves the tag one green (verified). Keep both, and read the tag selector's "also catches a NEW control added later" comment as covering tags only. **A containment contract has THREE ends, and each needs its own line: outside X, inside Y, and IN ORDER.** On BIN-660's EventCard, re-nesting is 3/12 red, but hoisting the toggle clean OUT of the `.ev` root (wrap the return in a fragment, close the root before the footer) left every test green until `expect(container.firstElementChild).toContainElement(toggle())` joined `link.contains(button) === false` (now 1/12 red alone). The third end is still open there and is the one authors always skip: MOVING the footer block to be the FIRST child of the root — so "markera sedd" renders above the poster — is 12/12 green (verified 2026-08-03), because both containment assertions are order-blind. Ask for `cardLink().compareDocumentPosition(toggle()) & Node.DOCUMENT_POSITION_FOLLOWING` (or a `lastElementChild` assertion) whenever the ticket's acceptance says "no visual change". Near-miss: a strict SUBSET of its sibling (BIN-611) DOES flip under mutation but proves nothing new — the real test is **can it fail ALONE?**, so NAME the mutation that fails it while every sibling stays green (BIN-641: `isRewatchWrite`→`status === currentStatus` fails only the series case, so that case is load-bearing, not a subset). Rewriting a subset as a DISTINCTNESS/TOTALITY claim (`new Set(v).size === 3`, `.every(v => ALLOWED.includes(v))`) is the same subset in a new shape when the siblings exhaust the domain (a 4-member `WatchStatus` union: per-input `toBe` cases IMPLY it). LOW: documentation, never coverage — and it is strictly WEAKER, not merely redundant, so **DELETING it is a legitimate cleanup, not a weakening**, which is the one case where "an assertion disappeared" is not the red flag. Protocol for reviewing such a deletion (BIN-646, worked live 2026-08-07 on `planQuickRateWrite`): restore the PRE-deletion test file (`git show HEAD:<test> > scratch`, `cp` it in — never edit the staged file in place) and run a mutant set against the REAL prod file, requiring that the deleted case NEVER fails alone; six mutants (each branch's literal swapped, the guard inverted, the ternary collapsed each way, an out-of-union typo) each reddened a sibling too, and the `=== 'sedd'` → `!== 'sedd'` SWAP left the distinctness case fully GREEN while siblings 2–3 caught it — a set of three verdicts stays a set of three when two of them trade places. That green is the proof the oracle was decoration. Two preconditions before crediting the argument: the siblings must pin EVERY input to an exact literal (`toBe`, not `toContain`), and the input domain must be genuinely exhausted (check the union's real member list in `src/types/domain.ts`, don't assume). Also confirm the diff is deletion-only for that file (`git diff --cached --numstat` showing `0  N`) so the surviving assertions are byte-identical. **"Is this new dedicated test redundant with an existing suite's blast radius?" needs the NARROW mutation, not the wholesale-deletion one**: deleting a whole guard term (`&& data.updatedAt != null`) can fail dozens of UNRELATED tests as collateral (every fixture missing `updatedAt` for its own reasons now mis-fires) without that number saying anything about the new test specifically — re-run with the REALISTIC regression instead (the term's boundary made precise: `!= null` → `!== undefined`, which only misclassifies an explicit `null`) and count again. BIN-640's `addedAtIsRepairable({addedAt:null, updatedAt:null})` case: full deletion failed 25 tests file-wide (noise); the narrow `!==undefined` swap — which specifically mistakes Firestore's latency-compensated "still-pending serverTimestamp() reads back as null" echo for a real value — failed exactly the 2 tests that seed `updatedAt: null` verbatim (the dedicated integration test plus its unit twin), proving the dedicated test earns its place rather than riding the deletion mutation's coattails. **A negative multi-assertion test can have SOME terms vacuous by the fixture's own construction, not just by the mutation**: BIN-700's `WatchlistPage.test.tsx` "offers no destructive controls in that state" (`items:[]`, default `view:'grid'`) asserts three controls absent — but the bulk "Ta bort" button only ever renders when `selected.size > 0` (nothing to select with `items:[]`) and the "Välj alla" checkbox only in the `table` view (fixture never switches to it), so BOTH pass regardless of whether the whole-page error branch exists at all; only the always-rendered "Välj" toggle discriminates (verified live: reverting the branch reddens the test at the "Välj" line specifically, never at the other two). Per assertion in a negative-multi-assertion test, check whether the fixture's OWN state (not just the code under test) can ever produce the element being denied — if it can't, deleting the code branch won't make that line fail either, and the assertion is decoration. **A PERIODIC emitter's "keeps reporting" claim is unpinnable by a single-tick oracle, and the emit-BEFORE-stop-check order is why**: BIN-815's build watchdog `setInterval` writes the heartbeat line first and only then evaluates `if (inFlight.size === 0 && networkFetches >= refreshBudget()) stopWatchdog()`, so the test named `skriver pulsen ÄVEN när ingenting är i flykt` — one 30s tick advanced after a fetch completes, then `expect(pulse).toContain('inflight=0')` — stayed green under the eager-stop mutant `if (inFlight.size === 0) stopWatchdog()` (verified live 21/21, 2026-08-07), which is exactly the regression that turns the ticket's headline diagnostic (a build hanging while the pulse keeps saying `inflight=0`) back into one line then silence. Advance a SECOND interval and COUNT: `await vi.advanceTimersByTimeAsync(TICK); expect(lines.filter(l => l.startsWith(PREFIX))).toHaveLength(2)` — verified red-alone (1/21) under that mutant, 21/21 green on the clean file. **CLOSED r2 (2026-08-07), and the close carries the idiom**: `advanceTimersByTimeAsync(TICK*3)` + `toHaveLength(3)` + a per-pulse `toContain` loop — eager-stop mutant now 2/27 red (repetition test among them), unconditional `stopWatchdog()` in the tick body 4/27. Two extra checks make the count-oracle trustworthy rather than a `>=1` in disguise, and both are owed on any "keeps emitting" test: (i) mutate the INTERVAL itself (`30_000`→`45_000` → 2 pulses ≠ 3, red, verified) so you know the number is counted and not merely non-zero; (ii) when the diff DELETES a stop/quiet rule and its tests, mutate the deleted rule back IN — re-adding `if (inFlight.size === 0 && networkFetches >= refreshBudget()) stopWatchdog()` was red-ALONE on the replacement test, which is what separates "behaviour removed, opposite behaviour now pinned" from "tests dropped to go green". A test file that hardcodes the unexported tick literal couples broadly (that one mutation reddened 12/27) — accept it, but expect the blast radius. Two companions from the same review: (a) **each FIELD of a diagnostic log line is its own term**, one probe per field exactly like the per-class rule above — `oldest=${…}`, `budget=${n}/${refreshBudget()}` and the STUCK line's `${Math.round(age/1000)}s` each survived being hardcoded to `0` at 21/21 while `done=${networkFetches}` was red-alone, so `toContain('inflight=0')` says nothing about its neighbours and an acceptance criterion reading "named with its `kind/id` AND ITS AGE" is UNMET by a fixture asserting only `kind/id`; (b) **a mid-test `__resetXState()` can render the whole narrated setup inert** — that same test opened with a hanging fetch plus one tick to stage an in-flight→idle transition and then reset it away, and DELETING those setup lines left the suite 21/21 green (verified), so the transition the name promises is never exercised. Probe any test whose setup precedes a state reset by removing the pre-reset lines and re-running: still green means the name is narrating, not testing. Conversely, a module-global registry + fake timers is NOT automatically leaky — a `void fetchForBuild(kind, () => new Promise(() => {}), id)` helper is safe while the promise never settles (its `finally` never fires) and `__resetXState()` runs in BOTH `beforeEach` and `afterEach`; the residual is that `void` swallows a rejection, so the day a fixture calls it past an exhausted budget the throw lands as an unhandled rejection on an unrelated test — ask for `.catch(() => {})`, don't block.
- **Defense-in-depth double validation hides the first guard, once PER VALIDATOR**: when a value is validated on WRITE and re-validated on READ (`rememberNextPath`/`takeNextPath`, BIN-645), any test asserting through the reader passes with the write-side guard deleted. Assert the INTERMEDIATE directly, never the re-cleaned output — same shape for any strip/normalize-then-reparse pair. Fixing it for validator #1 does not fix #2: pinning `safeNextPath`'s write half did not stop the NEXT round's read-side `stripUnsafeQuery` leaving the WRITE-side strip deletable at 34/34 green, in the test literally named `is applied by rememberNextPath, not just available to it`. So on any diff ADDING a read-side copy of an existing write-side cleaner, re-mutate the write side, and read a name of the form "is applied by X, not just available to it" as a promise to assert X's OUTPUT: `rememberNextPath('/grupper/X?invite=TOKEN'); expect(sessionStorage.getItem(KEY)).toBe('/grupper/X')` — verified red-alone, shipped.
- The converse acquits most mocked-hook component tests: `not.toHaveBeenCalled()` beside a positive `toHaveBeenCalledWith` in the SAME `it`, and a CALL-COUNT beside an ambiguous-text selector — `getAllByText('Sedd')[length-1]` reaching a menu entry whose TRIGGER carries the same label is safe ONLY because `toHaveBeenCalledTimes(1)` sits in that `it` (BIN-641, proved by renaming). Same for `mock.calls[n][1]` off a full-replacement `vi.mock('@/hooks/useWatchlist')`. PROVE by deleting the component's whole write path (`void addItem; void updateStatus;`, keep the state call): green = hollow, red = real (BIN-611). Purely-negative tests still need a positive — "skipping writes nothing" passed with `skip` neutered to a no-op. **A hook-side guard keyed on an ARGUMENT the component supplies is only as good as the caller's literal, and a "the affordance now exists" test pins none of it** — BIN-679 routes season 0 past the watchlist marker with `if (season === 0) return` in all three mark callbacks (each mutation-killed, hook-side), but `SeasonList.tsx`'s new specials checkbox carries the sole `markEpisodeWatched(0, ep.episode_number, w)` and its test only asserts `queryAllByRole('checkbox').length).toBeGreaterThan(0)`: swapping that `0`→`1` (guard bypassed, marker written, `syncProgressToGroups` fanned out to other members' spoiler boundary), replacing `onToggle={handleToggle}` with `() => {}`, swapping the read-side `isWatched(0, …)`→`(1, …)` and hardcoding the header's derived `watchedCount={0}` ALL survived the whole batch (verified live 2026-08-08, 47/47 and 17/17 green). Owed at the call site: click the checkbox against a spy and assert `toHaveBeenCalledWith(0, <episode>, true)` + `toHaveBeenCalledTimes(1)`, plus an `isWatched` fixture true only for `(0, N)` pinning the checked box and the `1/2` counter. And prefer `toHaveLength(<exact>)` scoped to the section over `toBeGreaterThan(0)` — the loose form also goes vacuous the day a neighbouring collapsed row starts rendering its own checkboxes. **CLOSED r2 (2026-08-08) — all four re-derived red-ALONE at 1/41 each — and the close carries the general rule: enumerate EVERY prop the new call site hands the REUSED shared row, and then every ARGUMENT that row's callback hands BACK.** A fifth mutant, `seasonNumber={0}`→`1` on the `EpisodeRow` inside the new specials row, survived 41/41: that prop feeds the `S0E83` code label AND `EpisodeReactions`' `season`, i.e. `episodeReactionKey(tmdbId, season, ep)` = `${tmdbId}_${season}_${ep}` — special #2 would post into `57243_1_2`, the same doc as S1E02 — and the diff's own comment staked a named stakeholder sign-off ("#18 Community Manager, godkänd: the thread key is `${tmdbId}_0_${ep}`, a fresh slot with no collision") on exactly that literal. **A stakeholder condition recorded in a code comment IS an acceptance criterion**: grep the staged comments for "reviewed by / godkänd" and mutate the literal each one names. CLOSED r3 by making the `vi.mock('./EpisodeReactions')` stub a `vi.hoisted` SPY that captures props (a `() => null` stub hides the namespace half entirely) plus `expect(screen.getByText('S0E83'))` — 1/43 red-alone, as are skewing `tmdbId={tmdbId}` and hardcoding `watched={false}`, and as is the sibling code-review fix `episode={withYear}`→`{episode}`. **r3's SIXTH mutant is the boolean the row hands back UP**: `void markEpisodeWatched(0, ep.episode_number, w)` → `…, true)` survived 43/43, because the file's only `fireEvent.click` lands on an UNCHECKED box — the tick direction is pinned, the UN-TICK direction of the same new affordance is not. Consequence is a stuck-on checkbox, no marker/cross-user reach, so LOW-MED not blocking; remedy verified 1/44 — in the checked-state case add `fireEvent.click(boxes[1]); expect(markEpisodeWatched).toHaveBeenCalledWith(0, 83, false)`. Same round, the FALSE branch of a derived-prop `useMemo`: `year ? { ...episode, name: \`${episode.name} (${year})\` } : episode` — dropping the `year ?` guard survived 43/43 since every fixture episode carries an `air_date`; one extra `it` whose `useTVSeason` payload has `air_date: null` is red-alone 1/44. **Loosening a POSITIVE matcher (exact string → regex) is legitimate only when the exact claim is re-pinned elsewhere, and that is provable, not arguable**: mutate the string in a way the regex still matches (prefix it — `Special: ${episode.name}`) and require the NEW exact test to redden ALONE (verified 1/43). Loosening a NEGATIVE `queryByText` to a regex STRENGTHENS it (it now forbids a superset) and needs no proof. **r4 CLOSED both (2026-08-08, re-derived in a fresh isolated rig: un-tick `w`→`true` 1/14 red-alone on the checked-state case; `year ?` guard dropped 1/14 red-alone on the new no-air-date case), and the SEVENTH mutant sits one step PAST the call site — in the shared row's OWN onward hand-off.** `EpisodeRow.tsx`'s `<EpisodeReactions … watched={watched}>` → `watched={true}` (open every reaction thread regardless of progress) survives the ENTIRE tree at 2683/2683 green, because `EpisodeRow` and `EpisodeReactions` have NO test file of their own and the batch's `SeasonList.test.tsx` is the only file in `src/**` that so much as names `EpisodeReactions` (`grep -rl <Leaf> src --include=*.test.tsx` is the cheap discovery step — one hit, and it is the diff's own file, means the shared chain is unpinned). The staged comment staked #18 Community Manager's sign-off on TWO clauses — the thread key `${tmdbId}_0_${ep}` AND "the reactions list only opens once YOU have marked the episode watched" — and only the first was mutated; **count the clauses in a `godkänd`/`reviewed by` comment and mutate EACH**. Pre-existing line, untouched file, so LOW not blocking — but the close is one line in the batch's own harness, since the hoisted spy already CAPTURES `watched` while the assertions read only `season`/`tmdbId`/`episode`: assert the fourth prop per row (`false` for the unticked fixture, `true` in the checked-state case). General rule: enumerate the props the new call site hands the shared component, the arguments its callback hands back, AND the props that component forwards to ITS children — the third list is the one nobody checks. Counter oracle: a `getByText((_, el) => el.className.includes('text-xxs') && el.className.includes('mx-4'))` scoped by STYLING fails LOUDLY (2 red), never vacuously, when the class moves — acceptable, but the VALUE form `getByText('1/22')` is strictly better (same kills, no class coupling), because RTL's `getNodeText` JOINS an element's direct text-node children, so `{watchedCount}/{total}` as three JSX children matches fine and a comment claiming otherwise is wrong. And a hardcoded curated-list denominator (`1/22` from `canonicalSpecials.ts`) is a HEALTHY tripwire, not brittle coupling: deriving it from `canonicalSpecialsFor(id).episodeNumbers.length` would be tautological, and a curated-list edit SHOULD force a look at a user-visible counter.
- **A test whose NAME lies is rename-and-annotate, not delete.** Shapes: misattributes a bug's origin; describes a MODEL the fix abandoned (BIN-608); two halves assert the same branch; a name claiming two behaviours whose fixture encodes one, or holding the defining input CONSTANT; a name whose fixture exercises only HALF the API it claims ("survives storage unavailable" spying `setItem` only, BIN-645); **a name LISTING several sibling functions as "the same for X, Y and Z" whose body calls only X** — a BIN-598-style mechanical sweep across N sibling mutators needs one fixture PER NAMED function, not one test claiming all (BIN-596: "the same for updateProgress, updateTmdbStatus and updateWatchedAt" drove only `updateProgress`; the other two's harness refs did not even exist, and reverting either's stale-closure fix left the whole 61-test file green — verified live). **A NEGATIVE clause ("only a re-subscribe clears it, NOT a later snapshot") must DELIVER the denied event** between trigger and assertion — BIN-601's fixture skipped it, so restoring the deleted reset left all 49 green; one added line makes the EXISTING assertion discriminate. Keep IF a plausible mutation of the NAMED function flips it; rename to what it proves. Delete only if NOTHING flips it (BIN-593). Compute a fixture's arithmetic before crediting its label (BIN-396).
- Empty-collection vacuity: `.every(e => e.kind === 'cancel')` as sole oracle is true on `[]` — assert length or a full `.toEqual([...])` first; every `offenders…toEqual([])` scan-guard needs a sibling non-vacuity test RUN against the real tree (BIN-356/370); set-equality needs `expect(setA.size).toBeGreaterThan(0)`. Single-candidate oracles are vacuous for priority/tier/exclusion/dedupe claims — need a COMPETING candidate the buggy logic would wrongly pick; likewise `.toBeLessThanOrEqual(CAP)` on a 6-element fixture vs a 15000 cap. Values computed by the fn under test are tautological — **and so is a real browser API written then read straight back with no production code in between**: `sessionStorage.setItem(K,v); expect(getItem(K)).toBe(v)` placed AFTER `render()` stayed green against a page that WIPED the remembered path for signed-out visitors (BIN-645). Being a genuine API, not a mock, is no defence — check the ORDER: seed, then run production, then assert. Moving the seed ABOVE `render()` reddens it, verified with a wipe-on-early-return mutation.
- Assert values, not shapes: `arrayContaining` is too weak for exact sets (compare as `Set`s); N-flag returns get a full-object `toEqual` per path (BIN-346); `toMatchObject` on a `merge:true` payload is blind to omitted-vs-null — use `toEqual`, or `not.toHaveProperty` per omitted field when a blanket `toEqual` would freeze a field under an open ticket. Same blindness at a CALL SITE off a mocked write hook (BIN-611): the builder's own suite pins the BUILDER, not who calls it. **Strong call-site form for "flag X must never reach the store" (BIN-641): a KEY-SET DIFF of two writes** — `expect(keysWith.filter(k => !ALLOWED_DELTA.includes(k)).sort()).toEqual(keysWithout.sort())` — three load-bearing conditions: (a) the payload getter asserts `calls.length > 0`; (b) the filter names EVERY key the feature legitimately changes AND the fixture SETS them (with no stored `watchedAt` both writes stamp it and the delta is never exercised); (c) prove the diff line fails ALONE — not by leaking the flag (a sibling `not.toContain` short-circuits first) but by making an UNRELATED key diverge only on the flagged path. OR-filters need operand 1 explicitly false and 2 true, never a factory default. Exclusion claims need a candidate excluded ALONGSIDE included ones. **A DETAIL→LIST shape-adapter's mapped field is proven only by piping it through the REAL downstream reader, not by asserting the adapter's own output** — `toRowTitle`'s `origin_country` (detail `origin_country ?? production_countries.map(...)` → list `RowTitle.origin_country`) closed a page-wide blocking gap this way (BIN-583): `useRowCompanion.helpers.test.ts` asserts the mapped value AND runs it through the real `applyClientFilters` for both the country dropdown and the saved "dölj länder" list, rather than stopping at `expect(t.origin_country).toEqual([...])` — the field being present says nothing about whether the one function that reads it twice actually finds it. Verified live (2026-08-07): mutating the field to `undefined` fails 3/5 in that file alone.
- **A React IDENTITY contract is pinnable and worth pinning** when a callback's dep list carries a value the body never reads (BIN-598: `setRuntime`/`refreshTmdbFields` list `items` so title-page effects re-run after the first snapshot). Shape: capture the ref, drive the state change, `expect(ref).not.toBe(before)` for EVERY callback named; mutate each dep-array term separately. Check the paired `// eslint-disable-next-line react-hooks/exhaustive-deps` sits on the line ABOVE the dep array — two lines up it suppresses nothing and the surviving warning invites redoing the regression; with NO disable, `exhaustive-deps` still only WARNS (CI blocks on errors), so a dropped dep is a silent ship.
- **No dep-array term is pinnable while a mocked hook returns a FRESH object per call** — `useRouter: () => ({ push })` re-runs the effect on every render, so the whole array is inert and any test that seems to pin it is vacuous (BIN-645: dropping `profileLoading` from `[uid, user, profileLoading, router]` left 36/36 green, and the obvious new fixture ALSO stayed green until the mock was hoisted). Order of work: hoist the mock to ONE stable object (`() => { const r = { push }; return { useRouter: () => r }; }`, matching Next's stable router) — re-check the latch case still reddens under it — THEN add the transition fixture. **A comment claiming "ONE router object, hoisted" over `useRouter: () => ({ push })` is wrong and worth catching**: `push` (the `vi.hoisted` spy) IS one stable reference, which is all a call/argument assertion (`expect(push).toHaveBeenCalledWith(...)`) needs — but the wrapping `{ push }` object literal is still fresh every call, so any `useCallback(..., [router])` inside the hook under test still recomputes every render and that dep term stays untestable. Harmless where nothing in the file asserts identity/memoization (verified live, BIN-714's `useSignedOutRedirect`: no test relies on `goToLogin` referential stability, so the gap is real but inert) — LOW, name it rather than block, and demand the FULL idiom (`const r = {push}; useRouter: () => r`) the moment a dep-array or re-render-count assertion is added to that file. **Two-phase async state needs the TRANSITION, not the settled end state**: a `profileLoading: true → false` where nothing else changes identity is the only state the term carries, so a test rendering already-settled (`profileLoading: false`, `user: null`) reproduces the verdict but never the production SEQUENCE. Verified remedy: render true, `expect(push).not.toHaveBeenCalled()`, flip to false, `rerender`, assert the push — red ALONE under the dropped dep, and equally red ALONE under the twin mutant the code reviewer will phrase differently (latch-on-the-way-in, `if (!uid || latched) return; latched = true; if (profileLoading) return;`): one fixture, two named mutants, re-derive BOTH.
- **Hoisting a mocked hook to a stable object CHANGES THE IDENTITY CONTRACT EVERY OTHER CASE IN THE FILE RUNS UNDER** — it removes effect re-runs, so it can turn a previously-RED mutation GREEN. Treat it like an added `disabled` attribute: re-run every latch/dep/gate mutation in that file against the new bytes rather than carrying the prior verdict (BIN-645 r3, all six still discriminated: latch 1/7 alone, `!uid` 2/7, `profileLoading`-in-gate 1/7, the `user != null` term 2/7, the `takeNextPath()` consume 5/7). Expect the count to GROW, never shrink; a shrink is the hollowing signal. Afterwards, some terms are **provably inert by construction** — with the latch present, dropping `user` or `router` from `[uid, user, profileLoading, router]` left 7/7 green, because the latch means only the first qualifying run matters and `profileLoading` already carries it (and Next's real router is stable too). Say that, do NOT file it; the residual is only that removing the inert `user` term silently hollows the latch case, which `exhaustive-deps` warns about and CI does not block. **A React Query `data` object is another provably-inert source**: a local `const derived = data?.field || fallback` computed fresh every render (not itself memoized) is subsumed by `[data]` alone in a sibling `useMemo` — React Query's structural sharing only preserves the `data` REFERENCE when a refetch is deep-equal to the prior result, so any change to `data.field` necessarily produces a new `data` reference too, and `derived` cannot change while `data` stays the same object. Verified on PersonPageClient's BIN-686 fix: `svBio = person?.biography || ''` dropped from `useMemo(..., [person, svBio])` to `[person]` is safe, not vacuous-by-omission — confirm the derived var reads ONLY fields of the already-listed query-data object, with no other independent state folded in, before crediting the collapse.
- **A "runs once per mount" latch (`redirectedRef`) IS drivable under vitest — never accept "I couldn't force a second effect run".** Two triggers, both verified (BIN-645): (a) a mock hook returning a FRESH object per call (`useRouter: () => ({ push })`) puts a new identity in the dep list, so the effect re-runs on every rerender; (b) assigning a fresh state object (`auth.user = { ...returning }`) before the second `rerender`. StrictMode double-invoke is a dev-only red herring. (a) is INCIDENTAL — with a stable mock and the latch deleted the case goes vacuous, so require the explicit fresh-value assignment. Standing two-step diagnostic on the TEST file (restore after), RE-RUN whenever the dep array or the mock changes: swap the mock to `() => { const r = { push }; return { useRouter: () => r }; }` and delete the latch — still RED means (b) carries it; then also blank out the fresh-value line — GREEN confirms which line is load-bearing and that the file's warning comment is true (re-derived on the stable-mock bytes: latch-deleted RED 1/7, latch-deleted + line-blanked GREEN 7/7). **Trigger (a) can fire from a component's OWN bootstrap effect, with no explicit `rerender()` in the test at all** — `AuthGuard.tsx`'s `useEffect(() => setMounted(true), [])` forces a second render on every mount, and its `AuthGuard.test.tsx` mock (`useRouter: () => ({ push })`) is the same fresh-object shape, so the guarded redirect effect fires TWICE inside a single `render()`/`act()` (confirmed by instrumenting a module-level counter: 2 runs per mount, verified 2026-08-04). Consequence: a dedicated test that calls `render()` a SECOND time — a second independent mount, not a `rerender()` of the first instance — to "prove the second pass also skips" adds nothing: it flips under a consume-once mutant, but never ALONE, always alongside the single-mount sibling that already double-fires for free. Diagnose with the same counter probe before crediting a "proves the re-run" test; if any single-mount case already double-fires, the dedicated one is a subset (bullet below) and its comment's claimed mechanism (Strict Mode / a rerun WITHIN one instance) should be corrected to name what actually discriminates, or replaced with an explicit `rerender()` on ONE instance if within-instance re-execution is the thing meant to be pinned. **A same-family case needs no rerender/mock trigger at all when the guard's own ref-latch has no memory of render COUNT, only of ref-nullness at first non-loading effect run**: BIN-748's `AuthGuard.test.tsx` "holds even when the guard mounts LONG after the verdict landed" mounts straight into `loading:false` (skipping the earlier `loading:true` render its `bootSignedOut()` siblings pass through first) to narrate DynamicRouter's late chunk-load mount — but `signedOutAtFirstVerdict.current === null` is checked identically regardless of how many renders preceded the first one where `loading` is false, so this case and the sibling "remembers NOTHING when this tab had already shown a session on this page" fail in LOCKSTEP under every mutation of the marker check (verified live 2026-08-05: reverting the marker term failed both together, 4/17, never one alone). Not vacuous — the assertions are real and the invariant they pin is real — but it is a DUPLICATE of the sibling under this component's actual structure, not an incremental proof of "late mount" specifically; call it LOW/redundant-documentation, not a gap, and don't credit its narrative claim without first checking whether the code path it claims to isolate is reachable only through extra renders (usually it isn't, once there's no per-render-count state involved).
- Coverage does NOT transfer between sibling output shapes, sibling COMPONENTS, or consumers of the same fix — mutate each (BIN-169/349/596); indirect coverage via an old consumer's test file is presumed NON-discriminating until mutation proves otherwise. Similarly-named "empty" tests ("empty INPUT" vs "FULFILLED-empty result") aren't automatically redundant (BIN-595).
- A cache-invalidation test must PRIME the cache first (BIN-510). An "unsorted input" test needs a fixture whose output CHANGES when the sort is dropped (BIN-185); `.sort()`-before-compare is blind to any order contract claimed (BIN-407). **A React-Query `queryKey` term IS a guard needing its own fixture**: pinning the movie/tv split at the DOC ID leaves `queryKey: ['x', mediaType, id]` free — dropping `mediaType` keeps every doc-id test green while movie N and TV N share one cache entry. Fixture (BIN-564, verified): ONE `QueryClient` hoisted out of the per-test wrapper, render both types against it, assert the second gets its own `checkedAt` AND read its own doc.

## Fixture-ordering discipline & filter→sort→slice pipelines
- Build fixtures so the WRONG implementation would win: first-wins candidate FIRST, true winner second for argmax; later/heavier value first for earliest-of-N; ascending input for a descending sort; NaN entry AFTER the valid max. Assert the winner's id AND the surviving subset, not just count. `.sort(localeCompare(...,'sv'))`: Set-insertion order must DIFFER from alphabetical (BIN-44). Ordering doesn't reach a lookup that PARTITIONS by key — prove inert with a reorder probe, don't file it (BIN-608). **A "report the N OLDEST + a count" reporter needs THREE probes, and the fixture must break every correlation it can**: BIN-815's watchdog registers 7 hanging calls 1 s apart, advances past the 30 s STUCK threshold so all seven qualify, then asserts `named).toEqual(['call-1'…'call-5'])` + `/och 2 till$/` — reversing the comparator is red-ALONE (verified, it names call-7…call-3), but two other mutants stay GREEN and you must say WHICH is a gap and which is not. Deleting the sort (`.sort(() => 0)`) is inert BY CONSTRUCTION — a `Map` keyed by an incrementing token iterates in insertion order, which already IS ascending `startedAt`, and ES sort is stable — so report it as equivalence, not a gap. Sorting by the WRONG KEY (`a.label.localeCompare(b.label)`) is a real regression the test cannot see, purely because the fixture's labels (`call-1`…`call-7`) ascend in the same order as their ages; in production the labels are `params:popular-movies/p1…p500`, where lexicographic order (p1, p10, p100…) is nothing like age order. Cheap remedy, always ask for it: name the fixture entries so alphabetical order CONTRADICTS age order (register `call-g` first, `call-a` last, expect `['call-g'…'call-c']`).
- Collision-prevention migration (`mediaTypeDocId`): distinct keys per type in every fixture = green while never exercising the collision — require one fixture seeding BOTH types at the SAME raw key, asserting the untouched one survives (BIN-560).
- **Legacy→namespaced read-path fallback over a doc holding a MULTI-WRITER map** (`swipes.votes`, BIN-569/608): a doc-level `byKey.get(k)?.votes ?? legacy.get(id)?.votes` lets the first post-cutover write shadow everyone else's votes. Fixtures: DIFFERENT participants per doc; direction pinned by a shared participant with DIFFERENT values. **Doc ids there are attacker-writable** — an ALIAS (`movie_042`) means the same number as a genuine key, so the parser takes only CANONICAL ids (BIN-618): mutate the leading-zero rule, the prefix allowlist AND one term of it, plus the OVER-rejection twin (bare `42` must still parse).
- Pipeline must-have set: all-exclusion path; one mixed batch with a pass+fail item per condition, sort varied by a distinguishing field; cap at default AND custom limit (BIN-88). Assert derived flags directly — sort order can coincidentally survive a branch typo. Filter+attribution: one full positive `.toEqual`, one `.toHaveLength(0)` per skip condition. A rating/weight scale bug travels with an ORDERING bug — test scale and branch precedence separately (BIN-511).

## Sentinel / key-absence assertions & merge-write payloads
- **Firestore `merge: true`: explicit `null` DELETES stored data; an ABSENT key PRESERVES it.** "Fill every field with a default" in a payload builder is almost always wrong at a merge boundary. Every helper feeding a merge write needs an omission-vs-null test per field; ask not "does this match the old code" but "was the old null/omit choice ever correct" (buildAddPayload shipped live data loss under a "faithful refactor").
- **A new intent term OR-ed in FRONT of a data-loss guard silently disables ALL that guard's other protections while its comment keeps claiming them.** Shape `A && (newIntent || existingStrictGate)`: BIN-641's `opts?.countsAsViewing ||` before `canAutoStampWatchedAt(...)` let a COLD LOAD stomp user-authored `watchedAt`. Mutating the new term both ways proves nothing — **re-run the EXISTING guard's own fixtures with the new flag true.** Unreachable-today is no excuse on a PUBLIC context API. A count half and a value-overwriting half are TWO features — full-revert-probe each (drop / force TRUE / force FALSE / raw-flag form), each reddening a DIFFERENT named case. **Each successive NARROWING is a NEW untested branch the prior round's mutations do not reach**: mutate the NEW gate back to the PREVIOUS one, not just to `true`/`false`/deleted (BIN-641 r3 re-gated on the counted OUTCOME; reverting to the r2 term left 2243/2243 green — the whole round unpinned). If green it owes a fixture where the two DIVERGE (a TRACKED NON-'sedd' title) that also SEEDS the protected field (`watchedAt: 2019-04-02` on a `vill_se` doc), else both gates stamp and it is green for the wrong reason. A subset assertion is fine INSIDE an `it` whose other assertion discriminates; the subset rule bites only when the WHOLE test is the subset.
- **Sibling stamps with ASYMMETRIC guards** (`addedAt` on `current` alone; `tmdbFieldsRefreshedAt` on `settled && !current`) need a fixture where the guards DISAGREE (cold load, no `current`) — an all-settled fixture proves only the shared half. A predicate with TWO structurally-different TRUE branches needs a fixture per BRANCH (BIN-595). **A THIRD state added later** (BIN-601's `listenerFailedRef`) needs its own fixture diverging on the IDENTICAL empty-items state; its THIRD write site — the per-uid RESET in the subscribe effect — needs an account SWITCH fixture (`rerender` with `authState.uid` moved to `u2`).
- **A ref mirroring state purely so an ASYNC CONTINUATION can see it** (BIN-617) is unpinned by every ORDERED fixture: land the sign-out BETWEEN the call's start and its settlement — deferred rejection, fire `authCallback(null)`, then settle. Pair with the per-tab cap: mutate the guard to `false`, `>=`→`>`, and the per-uid `Map` key to one shared key — all three flip once a SECOND account is appended. A counter incremented only inside `.catch` needs the SUPERSEDED outcome to separate count-on-attempt from count-on-failure.
- Conditionally-omitted fields: assert `'watchedAt' in result === false`, not a value check — catches wrong value AND spurious insertion (BIN-35/91); per conditional field, one written case + one `'key' in update === false` case (BIN-319). Recurring security/cost class: assert a payload does NOT contain `updatedAt` or a PII field. Two optional inputs → two DISTINCT sentinels. Allowlist/prune helpers: assert ABSENCE of a dropped field the fixture actually sets — value-checks on kept fields miss a spread-instead-of-allowlist regression (BIN-423); `toBe` for a pass-through.
- A per-group re-model pins BOTH this group's exact key set AND `not.toHaveProperty` on a sibling's (BIN-468). A field's null-on-other-status value can be an IMPLICIT status filter for readers — a merge write flipping it to omit-preserves needs those readers grepped for a now-needed `status===X` gate, given ONE shared home + its own test file (BIN-593/598 `seenWatchedAt`); mutating that helper must fail its own tests AND ≥2 consumers' — a consumer that stays green is equivalent-by-construction or untested, and you must say which.

## Extract-then-test & layering
- Any `firebase-admin` **or** `firebase-functions/*` import makes a file's exports unrunnable under root vitest. Fix: split pure helpers into a `*.helpers.ts` / admin-free `config.ts` sidecar, re-exported from the old module so existing suites stay green UNEDITED (BIN-326/543). Moving an export into an already-extracted admin-free file is a CI unbreak, not a weakening.
- Flag "should have been extracted" when ALL hold: a sibling `.helpers.ts` exists; the block is Firebase/React-Query-free; tests only construct the block's OUTPUT instead of driving its inputs (BIN-410/411). Inverse counts too: the fn's OWN test file exists but a behavior change lands zero cases there while a consumer's file picks up the coverage (BIN-618) — stale-canonical-home; PROVE it by mutating the fn and showing its own file stays green; fix = mirror the cases back. **A third inverse: the fn is exported, fully unit-tested, and simply unreferenced** — grep every export name against the consumer file(s) in the same diff. BIN-580's `isCanonicalSpecial` shipped 6/6-tested but `SeasonList.tsx` re-derives the same allow-list check inline via a fresh `Set` instead of calling it, so the two can silently diverge later with neither side's tests catching it. Low, not blocking (both paths are correct today, mutation-verified independently) — name it so a future edit to one side gets checked against the other. A helper extracted in the SAME diff is born with this hole when every case holds one operand at the interesting value (BIN-641: `rewatchFields`' five cases all pinned one side to `'sedd'`, so collapsing the predicate to `status === current` left its own suite 5/5 green). Mutate a two-operand predicate to its EQUALITY form and demand the equal-but-uninteresting pair (`('mina','mina')`, `('vill_se','vill_se')`) in the helper's own file. **A DELIBERATE divergence between the `src/` and `functions/src/shared/` copies of a mirrored helper is a comment-only invariant** until the copy that did NOT change pins the behavior it keeps. **CLOSED pattern (BIN-636, `mediaTypeDocId`): a same-directory `*.parity.test.ts` importing BOTH real copies (`import * as client from './x'; import * as server from '../../functions/src/shared/x'`, relative paths — `functions/` has its own tsconfig and can't be reached via `@/`) with three describe blocks — an AGREED set both sides must match, the diverging alias set each side must resolve OPPOSITELY, and a same-mechanism "still agrees on the non-diverged branch" case. A gap this file documents as deliberate ("Documented gap, not an oversight") is not permanent — BIN-646 closed one: `resolveTmdbId`'s string-`field` branch used to short-circuit on `Number('042')=42` before the doc-id branch ran, so an aliased doc resolved or not depending only on whether it carried the field; the close (2026-08-06) gates a STRING field through the SAME `CANONICAL_TMDB_ID` regex as the doc id (numeric untouched — `42` can't be spelled `042`) plus an INDEPENDENT boundary, `0` (`Number.isFinite(0)` true, excluded from `CANONICAL_TMDB_ID` on both branches). Verified live: reverting the string-field gate alone failed exactly 1/52; re-admitting `0` alone failed exactly 4/52 — different tests. Don't accept "genuine tightening" on this shape without mutating each boundary separately.** Mutate BOTH drift directions independently, not just the one the ticket names: loosening the tightened side (client here) AND tightening the permissive side (server here) must each fail loudly — verified live, reverting the server's regex to the client's strict form fails the "server STILL ACCEPTS" cases plus the cross-check that folds all aliases onto the genuine id, proving the net catches an accidental "resync" in either direction. When the divergence is guarded by TWO independent terms (BIN-618's digit-strictness regex AND a separate unknown-prefix allowlist), mutate each term alone — a fixture set can look exhaustive while one term's coverage rides entirely on the other's (verified: reverting only the regex here flips 3 of 5 alias fixtures, only the prefix guard the other 2, together accounting for all 5 with no overlap). **A fourth inverse: a brand-new sibling function landing in a file whose OTHER exports already carry dedicated tests inherits none of that coverage just by being adjacent** — BIN-656/686's `personDescriptionInput` (`src/lib/seo/contentFloorInput.ts`) shipped with real branching beside two siblings that each have a `describe` block in `contentFloorInput.test.ts`, and got zero cases (CLOSED 2026-08-01 with a mirroring block, all three branches red-alone). Byte-identical delegation in the caller (`generateMetadata`) stays exempt; that exemption never transfers to the adapter itself, and once a client stops hand-rolling and calls the adapter, the adapter's cases are the only thing pinning the client's mapping too.
- A `Promise.allSettled` fan-out is accepted untested pattern-copy ONLY while it merely degrades — an untested precedent doesn't grandfather NEW branching on top. PARTIAL vs TOTAL failure (rethrow, so RQ retry/Sentry fires instead of a false-success empty array) is real logic needing extraction+test (BIN-595/596).
- "Pure helper extracted, wiring untested" is accepted ONLY when production consumes the tested fn's OUTPUT verbatim, no re-derivation and no BRANCH of its own — a caller switching on the verdict (BIN-611) owes a call-site test. **Run the FULL-REVERT probe on EACH side of a two-sided feature separately**, grepping the helper's importers; the mirror side is routinely missed (BIN-645: reverting `login/page.tsx`'s `takeNextPath()` → bare `push('/')` left 2215/2215 green). A consumer owes all three outcomes: stored+normal → stored path; stored+higher-priority branch → that branch AND the value still CONSUMED; nothing stored → the default. **The consume half is the one authors ship without** — `push(needsOnboarding ? '/onboarding/' : (takeNextPath() ?? '/'))` stayed 5/5 green while the path survived to hijack the next sign-in. Adding `expect(sessionStorage.getItem(KEY)).toBeNull()` to the higher-priority case closes it, verified red-alone. **An OBSERVABILITY feature is two-sided and the REGISTRATION side is the one that ships untested** — BIN-815's build watchdog got 17 dedicated tests for the emitter (`buildFetch.ts`) and ZERO for the callers registering with it; reverting both caller files to HEAD left the WHOLE tree 2652/2652 green (2026-08-07). The emitter's own comment said the diagnostic is "only sound if every build-time call is registered" — an invariant stated in prose, pinned nowhere. Treat "the helper is well tested" as covering none of it and run the full-revert probe on the CALLER files as their own step. **CLOSED r3, and the close is the idiom**: a dedicated `src/app/titleParams.watchdog.test.ts` beside the callers, `describe.each` over the two routes, stubbing each page's client component (`vi.mock('@/components/pages/MoviePageClient', () => ({ default: () => null }))`, keeps Firebase out) and spreading `importOriginal` over `@/lib/tmdb/client` to swap only the list fetchers for never-resolving ones, then driving the REAL `generateStaticParams` under fake timers + `__setBuildFetchLogger`. Verified per-route, not incidental: unwrapping `trackBuildCall` in ONE route reddens exactly that route's 3 cases and spares the sibling's (3/6 each way), and mutating that route's label literal is red-alone (1/6) — module-global registry state cannot satisfy the other route because `__resetBuildFetchState()` runs in both `beforeEach` and `afterEach`. Two residuals this shape always carries, both LOW: (i) **a redundant "start the emitter" call placed in FRONT of a registration that starts the same singleton is inert by construction and unpinnable** — deleting `startBuildWatchdog()` from either route left 33/33 green, because the first synchronous `trackBuildCall` starts the same interval; say it is inert, do NOT demand a test, but DO correct the test name ("startar pulsen … innan något anrop hunnit svara") and any acceptance line claiming it "[mutation-verified]", since only the register half is; (ii) **one probe PER LABEL call site** — the fixture asserted `params:popular-movies/p1` and pinned only the first `collectIds`, so mutating the second call's literal (`'top-movies'`→`'zzz'`) stayed 6/6 green. And the phase matters more than the file: an observability probe wired into a caller that does NOT run in the failing phase (r2 instrumented `sitemap.ts`, which runs in *Generating static pages*, not the hanging *Collecting page data*) is worse than none — it makes `inflight=0` read as exoneration. Both halves are cheaply pinnable from the caller's suite: (a) `expect(vi.mocked(fetcher).mock.calls[0][1]?.signal).toBeInstanceOf(AbortSignal)` per fetcher for a timeout half; (b) a STUCK line containing the exact label for the label half — a label is a string the caller re-derives, so it needs its own assertion per caller file AND per call site, never one shared probe. **CLOSED r4 (2026-08-08), with the per-call-site idiom worth copying**: resolve the FIRST fetcher and hang only the SECOND (`first.mockResolvedValue(...)`, `second.mockImplementation(() => new Promise(() => {}))`), so the 5-entry STUCK cap cannot be filled by the first call's 500 registrations — mutating each route's second label literal is now red-ALONE (1/40 per route, was 6/6 GREEN in r3). Two more shapes settled the same round: (i) **a caller that registers an AGGREGATE** — person/[id] wraps `collectPersonIds` (~2100 underlying calls) in ONE `trackBuildCall('params:person-ids')`, which still discriminates per route (unwrapping it fails exactly that route's 2 cases, 2/40; mutating its label 1/40) and is the honest granularity for "this whole pipeline never returned" — but it FALSIFIES an emitter comment claiming "a STUCK line means the call outlived its own 20 s abort deadline", since an aggregate carries no deadline of its own; re-read that comment on every diff that adds a caller of a different shape; (ii) `it.runIf(route.second !== null)` inside a `describe.each` is the RIGHT way to exclude cases a route structurally cannot exercise (one label ⇒ no second-label case, no cap case) — the resulting skipped count is expected, not a `.skip` smell, provided the route's own registration and label still have running cases. And **re-count every numeric claim in the staged doc each round**: adding tests moves them — todo.md's "[mutant kills 4 tests]" for the unconditional-`stopWatchdog()` mutant measured 5/40 once the new ordering test (which needs a second tick) landed.
- **A UI CONFIRMATION that restates a write decision is a SECOND consumer of the same helper and owes the same divergent-state fixture** (BIN-641 r4, useMarkSeen's "omtitt räknad" toast — deleting its whole narrowing left **2246/2246 green tree-wide**; the divergent case is flag TRUE + a tracked NON-'sedd' title). "It asks the same helper the write asks" is not enough: the caller also HARDCODES arguments the write computes (`rewatchFields('sedd', …)` where the TV branch writes `'mina'`), so mutate every LITERAL passed into a shared helper, not just the gate. Watch for a fixture detail that silences the assertion entirely (`rating: 4` — without it `shouldPromptRating` diverts to `showRating`, the toast never fires and the case is vacuously green). A term the helper already subsumes is equivalence-by-construction: mutate it, say it is inert, do NOT file it.
- Don't accept "index.ts can't be unit-tested" wholesale: separate the stuck I/O from the extractable decision (BIN-463/464). Security guards in admin `.mjs` scripts have ZERO root-vitest coverage — say so. An invariant living ONLY in an un-extracted write-payload builder is not protected by the pure-half tests.
- Admin-SDK orchestrator otherwise stuck review-gated: factor its loop behind a narrow per-call port satisfiable by BOTH the Admin SDK and the client SDK against a real emulator (BIN-566). A `functions/src/**/logic.ts` "faithful copy" of a `src/**` pure fn needs EVERY copy checked (BIN-523/589).
- Mock only the documented choke point: `vi.mock(path, async (importOriginal) => ({ ...(await importOriginal()), oneFn: spy }))` — full-replacement mocks make assertions tautological (BIN-164). **Mock/fixture arity must track the real API**: `onSnapshot(ref, successCb)` omitting `errorCb` misrepresents the SDK, so a later fix using it gets ZERO coverage until the mock grows a 3rd param the test drives (BIN-596/601); a test's own doc-parsing helper must call the SAME functions the real read path does (BIN-618). Diff the mock's FIELD list against the real one, not just params: BIN-645's `useAuth` mock returned `{uid, loading, signIn}` where the context returns `{user, uid, loading, profileLoading, …}`, so `treats a signed-in user with no loaded profile as signed IN` encoded `user` ABSENT (not `user: null`) on a fixture verbatim identical to a superset sibling — unable to fail alone. Adding the field, and SETTING it per case (null here, a loaded profile in `beforeEach`), is what gives the case a state no sibling has; prove it with an OR-mutation (`uid == null || user == null`) that reddens it and spares all three siblings. **Full-module `vi.mock('@/lib/x')` on a WHOLE lib file (not one fn) is fine for testing the CALLER's orchestration, but grep for a sibling `x.test.ts`** — if it's the only test file importing that module and it fully replaces the module, the real file's own contract (branches, promise-settling, a `beforeSend`-style PII scrub) has zero direct coverage anywhere. It matters most exactly when the diff CHANGED that module's public contract in the same commit (BIN-657: `sentry.ts`'s `initSentry` went void→`Promise<void>`, and `global-error.tsx`'s new `.then()` orchestration hard-depends on it settling — proven by mutating the real `sentry.ts`'s no-DSN branch to return `undefined` instead of `Promise.resolve()`: full suite unaffected, and in prod that throws `undefined.then is not a function` inside the crash boundary's own recovery effect). Remedy: a same-commit `*.test.ts` for the mocked module mocking only ITS choke point (e.g. `@sentry/react`, not `@/lib/sentry`). **Same shape at a whole-CONTEXT-PROVIDER grain, not just a lib file**: BIN-669 added `isSigningOut`/a `signingOutRef`-guarded `signOut` try/finally to `AuthContext.tsx`, and the only new consumer test (`AuthGuard.test.tsx`) does `vi.mock('@/hooks/useAuth', ...)` — full replacement — while `AuthContext.test.tsx` (untouched in the same diff) never once calls `ctx!.signOut()`. Zero coverage anywhere of the actual guarantee the feature's own JSDoc claims ("lowered … so a bounce that slipped through cannot survive either"): verified live (2026-08-04) by moving `signingOutRef.current = false` out of `signOut`'s `finally` (so a thrown `firebaseSignOut`/`clearFirestorePersistence` leaves the flag stuck `true` forever) — 44/44 unaffected across every file touching the surface. Blocking whenever the untested half is a flag/latch a THROW can strand, not just a happy-path branch. Remedy: a same-commit `AuthContext.test.tsx` case driving a real `signOut()` — success, and one where the awaited call rejects — asserting `isSigningOut()` clears in both. **A `writeBatch().commit()` mock hardcoded to always resolve (`commit: async () => {}`, ignoring whatever its own `set`/`update`/`delete` returned) makes "mark the dedup ref before the write, not after a successful commit" structurally unverifiable** — moving the mark from before the async IIFE to inside a post-commit `try` block (reintroducing exactly the retry-storm an architect review blocked on, BIN-601/640) still passes every test, because the mock's `commit()` never actually fails; only an unawaited/unobserved rejection from the individual `set`/`update` call can be simulated, and production code never awaits those individually. Verified live (2026-08-02): a "not marking before write" claim was confirmed for outright REMOVAL of the mark (1 failure, as claimed) but NOT for the more realistic post-commit-success placement (0/65 failures). **CLOSED same day**: `commit` became its own `batchCommit` spy (cleared + reset in `beforeEach`), and the "never repairs twice" test injects the rejection at commit, not at the per-doc spy — re-verified live (2026-08-02 r2): moving the mark into `chunk.forEach(...)` inside the post-commit `try` now fails that ONE test alone (1/66), where it failed 0/65 before. Give `writeBatch()` a `commit: vi.fn()` the test can reject, not a hardcoded resolve, whenever a "survives a failed batch write" claim is made — this is now the worked example. **A component-test comment citing a mocked PROVIDER's internal timing/duration contract as the reason for an assertion is a claim about the PROVIDER, not the component, and mocking hides exactly that** — BIN-796's `DeleteAccountSection.test.tsx` asserts `toast.show` is called WITH an action object, commented "toast without one self-dismisses after 2500ms, with one after 6000ms (ToastContext)"; true of the real `ToastContext.tsx` (`action ? 6000 : 2500`), but `ToastContext` is `vi.mock`'d there, so the assertion only pins the CALL SHAPE (real and correctly red-alone under a "drop the action arg" mutant — verified live). The claimed 6000ms guarantee itself is a SEPARATE fact belonging to `ToastContext.test.tsx`, and that file's own `show()`-path tests never use fake timers on the plain (non-rating) branch — only `showRating`'s fixed 6000ms is timer-tested; mutating `action ? 6000 : 2500` to a bare `2500` left BOTH files' full suites green (verified live, 2026-08-07). Not blocking here (the call-site pin is real, the durations aren't part of THIS diff, and the risk is a pre-existing gap in an untouched sibling file) — but name it: any comment invoking a mocked dependency's internal behavior as justification needs that dependency's own test file checked for the SPECIFIC branch/boundary cited, not just checked for existing.

## Firestore rules testing
- Verify `assertFails` cases actually threw `PERMISSION_DENIED` in emulator logs (`--reporter=verbose`) — a silently-vacuous assertFails is indistinguishable from a real deny. Rules value-checks get live-mutation-verified on a scratchpad COPY of the rules file.
- **A Cloud Function writing a NEW field onto a `hasOnly()`-gated, client-writable doc needs a SAME-COMMIT rules-whitelist update** — merge-writes evaluate the FULL post-merge doc, so Admin-SDK bypass protects only itself, not the doc's next client write. Shape: seed a post-writer doc, assert an unrelated client write still succeeds (BIN-402).
- Forge-pin idiom: check the REQUEST doc's key presence — `!('f' in request.resource.data) || request.resource.data.f == resource.data.get('f', null)`. The stored-doc idiom (`!('f' in resource.data)`) is forgeable on any doc created before the field existed (BIN-365). An idiom fix across N OR-branches needs a per-branch deny test.
- Attribution: seed every OTHER whitelist condition correctly so a denial is attributable to the guard under test (BIN-24); seed the owner profile for identity-forgery denies so it isn't a null short-circuit. Multi-branch `allow update`: pair every deny with a positive twin, seed preconditions EXCLUSIVELY so only the target branch is passable, walk every branch (BIN-276).
- Field OMISSION is distinct from wrong-TYPE — implicit fail-closed via `hasOnly()` needs its own test; a shared "valid" positive fixture must INCLUDE every type-guarded field; new stamped fields get the type-reject + future-date-reject pair. `getAfter()`-dependent rules need `writeBatch`, never bare `setDoc` (BIN-25). Size caps: 99→100 allowed + 100→101 denied. Public-read/no-write: anon read succeeds after a disabled-rules seed; both writes fail (BIN-180).
- No-public-read proof: flip the owner's `isPublic` to true mid-test and re-assert the deny (BIN-164). A flipped `assertFails`→`assertSucceeds` is legit when the rule genuinely split, traced against the new rule text. **GDPR export/deletion must never depend on LIST permission for own-doc access** (BIN-184).
- **A denormalized per-item field can go stale vs its profile source of truth** when the cascade write stamping it dies partway. Do NOT prescribe the fail-closed profile `get()` any more: Malin CANCELED BIN-609 on 2026-07-30 (per-read cross-doc cost vs the 25 SEK cap) and `accepted-deviations.md` now forbids re-filing "the visibility read rule fails open" — the accepted mitigation is BIN-587's pending-flag + retry. With a per-item override (BIN-595), pin only that the profile gate can NARROW access. **Firestore subcollections SURVIVE parent-doc deletion** — seed and assert every one (BIN-347). `collectionGroup` queries need an explicit path-shape scope guard.
- A rules test hand-building a batched write proves the RULE denies it but not that the real function stays un-batched — pair with a unit assert it never calls `writeBatch`/`commit` (BIN-556).
- No rules-test update is owed when `git status` shows no `firestore.rules` change and a grep of the changed collection against the rules diff shows zero overlap. A comment asserting a rules-safety OR a REACHABILITY claim gets verified against the actual predicates, never trusted, and **re-verified every round it is rewritten, including a claim stated as a PROPERTY** ("a caller that does not state intent must not count, reachable or not" still carries a residual factual half — grep that too). BIN-641 burned four rounds of these (a cited `OnboardingFlow.handleAdd` caller an `alreadyAdded` chip makes unreachable; a correct sedd→sedd claim resting on every `updateStatus(...,'sedd')` caller). Re-grep the LIST each round; it changes.

## GDPR, PII, deletion & money guards
- Deletion-cascade correctness is proven by RUNNING the emulator rules suite, not by tracing refs into the delete batch. A compile-time field-coverage gate is classification ONLY; pair with skip-set pinning (BIN-328). Greps prove PRESENCE, not deletion reachability. New user-owned subcollections flow through `KNOWN_USER_SUBCOLLECTIONS`. A destructure-before-write strip needs a key-absence assertion (BIN-505). Cross-check a shipped schema/listener against its binding review-panel plan doc — tests get "deliberately extended" to legitimize a vetoed design.
- Money/catalog: never hardcode a live catalog-derived price — compute it from the lookup at assertion time (recurring BIN-403). Alias-map mirrors (functions vs src): derive expected from the single source of truth, mutation-test BOTH drift directions (BIN-420). Resilience claims get checked at CALL SITES (BIN-430/442). A test hardcoding an unexported const must NAME it in a comment.

## Legitimate update vs cover-up
- Legit: the diff visibly implements the new contract, assertions stay strict, no unrelated assertion touched (BIN-16/17). **A strict→REGEX relaxation is an honest environment accommodation, not a weakening, only when THREE things hold and you PROVED the first**: the framework really does normalize the value (probe it — swap the exact form back in and READ the `Received:` line; `next/link` renders `entryHref()`'s `/tv/1399/` as `/tv/1399` in jsdom), the exact string is still pinned in the producing helper's own test (`src/lib/calendar/entry.test.ts` `toBe('/tv/100/')`), and the component consumes that helper VERBATIM with no re-derivation. Check the loosened form still discriminates what it names (`/^\/tv\/1399\/?$/` still pins media type and id). Strict→different is legit only when production changed FIRST for a real reason — the old behavior genuinely gone (no dead sort/cap/slice, or the asserted line DELETED) AND the new fixture fails if it returned (BIN-360/601). **A test rewritten to assert the OPPOSITE (the strongest form of this) needs four things, all four checkable in one mutation pass** (BIN-679, 2026-08-08, both accepted: `{season:0,episode:2}`→`toBeNull()` and "ZERO checkboxes"→"checkboxes exist"): the reversal is (i) equally strict, not looser; (ii) accompanied by a NEW case so the file GROWS (+1 `it` here, the un-mark path the reversal opens); (iii) red against the OLD behaviour re-applied — mutate the new guard OUT and require the rewritten case to fail (4/37 here); and (iv) the ban's stated REASON is genuinely dead, verified in code and not from the diff's own comment. On (iv) also grep for OTHER live paths to the banned state before crediting a "this was unreachable dead code" claim — season 0 was already tickable via `SeasonPageClient` on a hand-typed `/tv/<id>/sasong/0/`, and `useMarkSeen` still writes `last_episode_to_air` (a special for Doctor Who 2005) straight into the marker, so the reversal was a live FIX on more paths than claimed rather than the activation of dead code. Cascade row: every full-array `.toEqual` must GROW — shrinking is the cover-up signal (BIN-175). Retiring a fn: old export gone EVERYWHERE (BIN-178); dropping assertions for a schema-removed field is correct (BIN-292). VERDICT vs OUTCOME conflation (judged `clearable` vs committed `cleared`) can look like a weakening — trace the increment SITE; a pass-through cross-check is vacuous (BIN-566). A `deleteDoc` bump 1→2 is legit when BOTH exact paths are pinned (red flag: downgrade to `toHaveBeenCalled()`); guard+migration ship atomically (BIN-356).
- **Reverts get their own protocol, not mutation-testing:** blob-hash the index against the revert target; diff exported-symbol lists; test-count delta must equal the removed prod tests. Zero-test code reverted removes zero coverage. **In-batch revert** (undone pre-commit) leaves no trace in `git diff --cached`; confirm via `git show HEAD:<path>` and mutate the REJECTED design back in (BIN-593).

## When NO new test is owed
- One-line non-branching discriminants in hooks (`!snap.exists()→'ghost'`, bare `catch→null`) over a tested pure helper (BIN-21); prop-threading, conditional JSX, ARIA spreads, Set toggles (BIN-42); `useMemo`-only optimizations, zero-logic components (BIN-336); a JSDoc MOVED to the fn it describes or CORRECTED to drop a symmetry the code lost (BIN-616/618) — verify by running both copies. Byte-identical delegation to a tested helper (incl. a filter clause inert because a downstream `continue` already dropped it — READ that fn); a signature change threading its new param; pure I/O consolidation over a tested decision fn (BIN-541). Forward-prep with no reachable path; a defense-in-depth guard on a provably unreachable condition — don't demand a first RTL harness over one boolean loading-gate (BIN-596/595/601).
- When such a gate DOES get an RTL harness (BIN-596/645), mutate each layer separately — `current = notReady ? null : getItem(...)`, `disabled={notReady}`, each handler early-return, the refusal toast, the signed-out route (removed AND wrong path), BOTH terms of the auth gate. Mirror fixtures into BOTH siblings (QuickAddButton + StatusButton). The signed-out test must assert three things together — route taken, remembered return path (exact string, `.search` included), and the OLD consent-bypassing `signIn()` NOT called — and keying `signedOut` on the Firestore profile instead of `uid` must go red (verified BIN-645). Once `disabled={authLoading}` lands, BOTH in-handler auth guards (`signedOut`'s `!authLoading` and `handleRemove`'s copy) are unreachable — don't call either load-bearing, and don't file the green mutation; see the disabled-button rule. **"Exact string, `.search` included" must be checked PER SIBLING, not once per pattern**: BIN-668 copied the same `rememberNextPath(window.location.pathname + window.location.search)` handler into TopbarActions AND HomePageClient's landing CTA, but only TopbarActions' fixture set a query (`/search?q=dune`) — HomePageClient's fixture stayed on bare `/`, so dropping `+ window.location.search` there passed 8/8 green (verified live). Low severity only because the home route never carries a meaningful query today; flag it, don't block on it, and re-check the moment `/` grows a query-driven view state.
- **Mocking the provider hooks pins the COMPONENT and leaves the PROVIDER's state machine unasserted** — `vi.mock('@/hooks/useWatchlist')` full-replacement is fine for exactly that scope, no more. Mutate the provider facts the component tests assume: `useState(true)` for `loading` → `false`, and any "deliberately stays true" branch (BIN-601's onSnapshot error handler) → `setLoading(false)`; both left 49 green. Owed: `loading` assertions in the file rendering the REAL provider — true before the first snapshot, false after, false for a null uid, still true after an error callback. **A DERIVED boolean the provider exposes (`libraryKnown = snapshotSettled && !listenerFailed`) must be mocked as a GETTER computed from the same two primitives, never a settable literal** — a literal lets a test set `listenerFailed: true` and still hand the component `libraryKnown: true`, a combination production cannot produce, silently voiding every gate assertion in the file (BIN-596's CollectionSection/QuickAddButton mocks both do this correctly: `get libraryKnown(): boolean { return this.snapshotSettled && !this.listenerFailed; }`, byte-identical to `WatchlistContext.tsx`'s own derivation — grep-diff the two formulas, don't just eyeball them). Being getter-only also makes an accidental `watchlist.libraryKnown = true` in a test throw (strict-mode ES modules) rather than silently no-op. Confirm reachability, not just the formula match: check each of the 4 boolean combinations is independently producible by the real onSnapshot success/error callbacks — the easy-to-miss cell is "listener fails before ever settling" (`snapshotSettled: false, listenerFailed: true`), reachable when the FIRST snapshot attempt errors. Verified live 2026-08-04 on CollectionSection.test.tsx: comprehensively reverting every `mounted && libraryKnown` gate to bare `mounted` in the production file fails 3 of its 5 dedicated tests alone (not the 2 a prior report claimed — always re-run the count rather than inheriting it, even when the direction is "more covered than claimed").
- **Two exceptions override the exemption above:** (1) the ticket lists a test as an acceptance criterion — an unmet self-imposed bar is a gap even in an exempt shape; extracting the buried decision (BIN-599 → BIN-611's `planQuickRateWrite`) is only HALF the remedy, since the helper's verdict is inert until the CALLER is pinned obeying it. (2) the gate protects a counter/date the code calls permanent-and-uneditable (`rewatchCount`, `watchedAt`) — earns a test at one-boolean size; reserve the exemption for gates whose failure is a redundant render, not silent corruption.

## Rounding, jsdom, TZ/DST
- Rounding oracles need `.5`-exact fixtures (`79/2=39.5`: round=40 vs floor=39) (BIN-87); `Math.round(x*2)/2` with only an `x>0` guard snaps 0.25 to 0 — probe `0.1`. jsdom `getBoundingClientRect()` is always a zero DOMRect.
- **`fireEvent.click` on a `disabled` button fires NO handler**, so adding `disabled={cond}` RETROACTIVELY hollows out an existing case's post-click assertions: a mutation that used to go red goes green. Verified twice (BIN-645). So on any diff adding `disabled`/`aria-disabled`, RE-RUN the prior round's handler mutations rather than carrying the verdict. Usually NOT a gap: the in-handler guards become unreachable behind an attribute the same case pins (`toBeDisabled()` goes red if removed) — accepted defense-in-depth — but the comments must stop claiming to prove handler behaviour.
- A DST guard is real only if the buggy fixed-ms path yields a DIFFERENT calendar date at that fixture — spring-forward never crosses local midnight (tautologically green), so anchor across FALL-BACK (Sun 2026-10-25) and assert the exact ISO under `TZ=Europe/Stockholm`. Floor BOTH sides to local midnight for whole-day diffs (BIN-145); pick an instant where zones disagree (23:30 UTC = next day CEST) (BIN-105/350).

## Parsers, regex, dispatch, lookup tables & SEO
- `\d+` parsers: test the syntactically-valid-but-semantically-invalid boundary (`/movie/0` → null); strict-digit hardening needs empty-string and phantom-id-0 cases (BIN-560). Regex tightening: a NEGATIVE + POSITIVE pair per pattern — negative-only passes even if the regex reverts (BIN-176); anti-over-rejection tests are as load-bearing as the reject side. `\b` after a hyphenated token false-matches `accent-soft` — use lookarounds.
- Ordered dispatch: the ordering test uses a path matching an early branch correctly and a later one incorrectly (BIN-345). `as Record<string, any>` trips CI's `no-explicit-any` — cast the narrowest slice; `npx eslint` new test files (warnings OK, errors block).
- Alias tables: one exhaustive identity loop (every entry × every alias) covers current AND future aliases (BIN-64/90). An ALLOWLIST needs a fixture per MEMBER, not just per rejected value: BIN-645's `RETURN_QUERY_KEYS` was reject-side pinned (adding `'invite'` → red) yet DELETING `'row'` left 25/25 green while killing that surface's return path. Name every member in the keeps-it case; one added `expect` makes the deletion red-alone — re-derive PER MEMBER, not once (`q` reddens 2 cases, `status`/`provider`/`row` 1 each).
- **A URL/route fixture is a factual claim: grep it back to the line that BUILDS it.** Plausible-looking invented routes recurred EIGHT times across BIN-640/645, once inside the fix for the previous one. Check three things per fixture — the route directory exists under `src/app/`, the query KEY is read by that page, and the query VALUE is one the app can actually produce: `/rekommendationer/?row=2` was wrong on all three (`RecRow.tsx` builds `` `/recommendations/?row=${encodeURIComponent(rowSpec.rowKey)}` `` and `rowKey(id)` is colon-joined, so `?row=person%3A1245`), and `/my/all?status=sedd` was wrong on two (`NumberedActionsList` builds `/my/series?provider=N&status=behind`; `WatchlistPage` only honours `?status=` when `status==='mina'`, i.e. `/my/series`). A fixture that names a surface the app never had keeps passing after that surface moves, so it pins nothing. Curated domain id lists get cross-checked against external ground truth AND the enums/label maps they derive from. Sitemap: two-sided enumeration with REAL constants + an auth/noindex-leak scan (BIN-337/305). **Same discipline for a rejected-Error fixture feeding a message-sniffing branch (`err.message.includes(CODE)`): grep the fixture's string back to the line that actually THROWS it, not to the constant's declaration alone.** BIN-777's `DeleteAccountSection.test.tsx` first shipped `new Error(STALE_SESSION_PREFLIGHT)` — just the bare code — when the real thrower (`AuthContext.tsx:991`, `` throw new Error(`${REQUIRES_RECENT_LOGIN} (${STALE_SESSION_PREFLIGHT}): …`) ``) puts BOTH codes in one message on purpose, specifically so a caller sniffing in the wrong order can't tell them apart. A fixture with only the narrow code is reachable under EITHER branch order, so the swap mutant (check `REQUIRES_RECENT_LOGIN` before `STALE_SESSION_PREFLIGHT`) survived 5/5 — closed by rebuilding the fixture as the literal template string the thrower emits (verified live: swap mutant now fails 1/5 alone). Two sibling messages sharing an identical PREFIX (here, a promise-bearing sentence appended only on one branch) additionally want the differing suffix asserted on its own (`toContain` the promise clause / `not.toContain` it) so a prefix-preserving swap can't hide behind the shared substring.

## When to consult the archive
Grep `binge-test-reviewer.knowledge.archive.md` when: a gap resembles a past one; a bullet is too compressed to act on (which mutation/fixture, what the emulator printed); you're on a re-attempt/revert/Phase-N; or a test looks weakened-but-plausible on rules/PII/deletion/money.
