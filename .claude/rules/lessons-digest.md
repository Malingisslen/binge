# Lessons Digest (auto-loaded)

One line per lesson in `tasks/lessons.md` — this digest loads every session so corrections
are always in force; the full entries (Trigger/Rule/Example) are the deep reference.
**Sync contract:** every new lesson gets its one-liner appended here in the same edit.
The `knowledge-freshness` Stop hook (workflow-guards plugin) warns when the counts drift.

- [Workflow] Never bundle `docs/workflow-map.html` edits with feature code — a feature-revert silently drops unrelated flow docs bundled in the same commit, and the coverage linter stays green (it checks paths exist, not description content). Bit us 2026-07-10 (e2cf608 revert wiped BIN-422/423 flow prose). Keep map edits in their own commit. CI guard tracked in BIN-459.
- [Workflow] In a parallel sprint the review markers can cover only ONE batch and silently leave the sibling un-reviewed — the gate keys on marker mtime, not content. Post-sprint, READ each marker and confirm it names EVERY batch's surface; a fresh mtime with stale or scope-limited text is an unmet gate. Re-review the un-named surface (opus, small diff) and re-stamp naming it before commit. Bit us 2026-07-14 — specifics in tasks/lessons.md.
- [Testing] Never `git checkout -- <file>` to undo a deliberate test-mutation when the file also carries uncommitted real work — it restores to HEAD and wipes the real edit too (bit BIN-522, 2026-07-16). Snapshot the pre-mutation file (cp to scratchpad / scoped stash / reversible patch), then `git diff` afterwards to confirm the real edit survived.
- [Workflow] A review-gate ticket isn't done until its marker NAMES the surface it was filed to protect — a scope-limited `security-done.marker` that reviewed a neighbouring file (BIN-472: covered availableNotify, said "retentionCleanup NOT covered") is an UNMET acceptance, not a pass. Read the marker, don't trust its existence; file a blocking follow-up (BIN-476) and hold the deploy. 2026-07-11.
- [Workflow] "I read very little of what you reply" is a CONFIG bug first — grep the always-on setup (output styles, SessionStart plugins, stale model-tuning blocks) before apologising in prose; fix the mechanism (`~/.claude/output-styles/malin.md`) and delete the conflicting prose in the SAME edit. Long answers become an HTML page via the `report` skill, not a wall of terminal text. (2026-07-25)
- [Data] A legacy→namespaced doc-id migration must merge at the VALUE level, not pick a document — if the doc holds a MAP many writers append to (Tillsammans `swipes.votes`), a doc-level `?? legacy` fallback means the first post-deploy write creates an almost-empty new doc that permanently shadows everyone else's entries (BIN-569, 2026-07-28: yes-counts collapsed, a spent veto became un-re-castable). Spread-merge per key, fold legacy forward on write, and seed DIFFERENT participants in the two docs or the test can't tell the two fallbacks apart.
- [Workflow] A ticket body in the past tense ("now implemented", with a file list) is NOT evidence the code shipped — grep the tree for the target symbols before classifying anything obsolete or skipping it. BIN-569's body described its own fix as built; none of it was at HEAD, a prior sprint had written it up and never landed it. The grep-of-main premise check cuts both ways. (2026-07-28)
- [Testing] Vitest's on-disk transform cache (`node_modules/.vite/vitest`) serves the PREVIOUS mutant after a source restore — three agents produced fabricated mutation results this way on 2026-08-01 (a "dead" guard at 7/7 green that was load-bearing). `rm -rf` it before EVERY mutation run AND the clean control. Same family: `git diff --ignore-cr-at-eol` ignores a trailing CR but not a lone one (phantom diffs on CRLF→LF), and an LF-assuming patch tool in a `core.autocrlf=true` worktree exits 0 having changed nothing — indistinguishable from a surviving mutant. Assert the patch landed.
- [Testing] A test that pins only that a wait STARTS is blind to the mutant that never ends it — BIN-645's login gate looked covered by "render with profileLoading true, assert no navigation", but a latch-on-the-way-in mutant passed it while stranding every returning visitor. Drive the whole transition: hold → flip → rerender → assert the action fires. That same case is usually the only thing pinning the flag in the dep array (exhaustive-deps only warns). And hoist the `useRouter` mock to ONE object — a per-call factory makes every dep-array assertion in the file vacuous. (2026-08-01)
- [Workflow] A batch you cannot reverse-apply holds the ENTIRE sprint hostage — withdrawability DECAYS, so `git apply -R --check` every batch patch right after apply and BEFORE any post-apply fix touches the tree. An unattributed working-tree edit (or a bare CRLF delta git refuses as "does not match index") makes a failed batch unremovable, and 6-of-8 files reversing is the half-withdrawn state that must never ship. On failure, rebuild from clean HEAD with only the SURVIVING batches — never force it, never hand-normalize to make it apply. `typecheck` passing describes the tree WITH the failure still in it. Cost the 2026-08-01 sprint all six batches (BIN-683).
- [Workflow] A gitignored target is invisible to every diff-based gate, so the ticket EVAPORATES instead of failing — `.claude/**` work can't be written from a worktree (isolation guard) and doesn't appear in any `git diff`, so "no diff" reads as "no work" to every gate. `git check-ignore -q <path>` at SELECTION time; a hit means Tier D + a comment with the one-line remedy, never a batch. BIN-585 was silently dropped by two consecutive sprints this way (BIN-684).
- [Workflow] A batch that produces NO artifact is indistinguishable from one never selected — "no diff" reads as "no work" to every gate, so a whole batch can evaporate silently (2026-08-03: BIN-664/659/669 left no patch, no stash, no note, no comment). At close-out, assert one of {commit, patch, stash, written failure} PER selected batch before reporting anything. Date-namespace the patch files: `batch-N.patch` is unique only within one run, and a prior sprint's leftover sitting in the missing batch's slot makes recovery restore the WRONG work under the right name. And when work exists but didn't land, write "built, recoverable from stash@{N}" in the ticket — that is a different instruction than "not attempted". (BIN-707/708)
- [Testing] A sprint worktree has NO local `node_modules` (resolution walks up to the main checkout), so `rm -rf node_modules/.vite/vitest` is a silent no-op there — and deleting the SHARED cache is unsafe while sibling agents run. Get the guarantee directly instead: `grep -n MUTANT <file> && npx vitest run <test>` as ONE command, so "the patch landed" and "the suite passed" are one atomic record; restore from a scratchpad snapshot, never `git checkout --`. Related: `vi.getTimerCount()` can't prove a hook cleared its own timeout when React/React Query keep unrelated timers alive — spy on the faked `setTimeout`/`clearTimeout` and assert the specific handle. (2026-08-03)
- [Workflow] A tool call the classifier BLOCKS still poisons every subagent you launch afterwards — a refused edit to shared automation (`C:/claude-plugins/**`, hooks, gate scripts) stays in the transcript, and workflow subagents are sidechains of that session: on 2026-08-03 five of them re-attempted the same refused engine edit and were each blocked, including every APPLY and COMMIT agent, so 4 tested batches reached patch files and 0 shipped. Never edit shared infra from a session about to launch subagents; if one is refused, treat the session as contaminated for that topic and route around the defect in DATA (here `args.config.cleanTreeIgnore`). Also: the precondition agent ran `git status --porcelain`, got EMPTY output, and still reported a dirty file — when an agent contradicts a command you can run yourself, run it yourself.
- [Workflow] A patch file whose hash matches another patch already in the directory is NOT an artifact — "nothing was built" and "the work is done" are identical to any existence check. The 2026-08-03 sprint selected BIN-704/707/708 (batches that vanish) and then lost the trace for half its OWN batches: `batch-3.patch` was byte-identical to the prior run's patch, `batch-2.patch` to its neighbour, and BIN-638's real green work sat STAGED in the index in no bucket at all. Hash every batch patch against every other at close-out, disqualify duplicates from satisfying the per-batch assertion, and require `git status --porcelain` empty before writing a single Linear transition — staged-but-unaccounted is invisible to the report AND destructive to the next run (BIN-713).
- [Workflow] A per-ticket outcome verifier handed the WHOLE batch's diff writes the batch's verdict under one ticket's name — BIN-701 (a one-line comment change, no executing code) came back `data-safety=fail` and withdrew all of BIN-596/598/617/701, while BIN-705's grading recorded the same ticket as PASS. Slice the diff to the ticket's own hunks before dispatch; a verdict whose severity is impossible for the diff's size is a mis-attribution signal, not a finding. And write a FAIL onto its ticket the moment it is issued — a verdict living only in run state can't be contradicted or even noticed (BIN-712).
- [Workflow] A `top`-tier ticket handed to a worker that CANNOT convene the stakeholder panel loses the panel silently — BIN-732 (AuthContext, tier top, Legal+DBA required) was dispatched to a batch agent with no subagent capability; it self-noted the roles' stakes and flagged the panel "still owed" at the commit gate, where the only remedy left is discarding finished code. No security reviewer ran anywhere in that sprint, and batch 0's green gates would have been the ledger's only evidence. Check the tier against the WORKER's capabilities at dispatch, not at commit; and read review evidence per-FILE — "three reviewers ran today" says nothing about a batch whose files none of them opened. (2026-08-04)
- [Workflow] A sprint that commits per-batch must TRANSITION per-batch too — 2026-08-05 pushed four batches to main and wrote zero Linear transitions, leaving all six shipped tickets in `Todo` stamped with the SELECTION window, so the next sprint could re-pick and rebuild them. The BIN-707/708 evaporation class run backwards. Assert `{commit, transition}` as a PAIR per batch, written by the committer, not by a single trailing close-out step that one crash deletes (BIN-754).
- [Workflow] "Not built this sprint — needs your decision" is a BINDING parking brake, and a note the sprint writes into a ticket's description is not the founder answering. BIN-700/643/729 were parked as one unmade UX choice with three "Jag gör inget här förrän du valt" comments; the 2026-08-05 sprint built option (a) anyway and self-recorded it on BIN-643. It is live on main, so a different answer is now a revert, not a design choice. `list_comments` before implementing; a parked marker is an exclusion, never a downgrade.
- [Testing] A mutation run in a SHARED worktree can be reverted mid-run: assert the mutant BEFORE and AFTER the suite, in ONE command. BIN-624's guard-strip came back 244/244 green — reading as "dead guard" — because a sibling reviewer's restore landed between the pre-assert and the emulator's read; caught 3× in one review. `firestore.rules` is named by `firebase.json`, so an isolated run needs its own config + project id + port; restore from a scratchpad snapshot and verify by HASH, never `git checkout --`. (2026-08-05)
- [Workflow] A capability check that lives INSIDE the worker fires too late to be a gate — 2026-08-05b routed four tickets (BIN-766/760/468/689) to `medium`/`single`, handed them to batch agents that structurally cannot convene a critique, and each one discovered that AFTER building; all four were withdrawn at the commit gate. BIN-744 is recorded as shipped and installed exactly this check — in the worker. A precondition on whether work may START belongs to the party handing it out: run `docs/org/route.mjs` in SELECTION, match the tier against the planned worker, drop or re-route anything above `skip` before spawning. "Build it and park it" spends a slot to make code nobody can commit. When auditing a shipped rule, check WHERE it runs. (BIN-776)
- [Workflow] Never write "acceptance criteria met" into a ticket from inside the build — BIN-766's batch agent `save_issue`d three filenames and "Acceptanskriterierna 1–3 är uppfyllda" into the description; the batch then failed verification and was stashed out, leaving a Todo ticket that described itself as finished. The BIN-569 lesson running FORWARD: this time the sprint authored the false past tense. Mid-build ticket writes may only say "designed/attempted/rescoped, NOT committed"; completion claims are written by whoever holds the commit sha and carry it. Withdrawing a batch includes correcting the text that batch already wrote. (2026-08-05)
- [Design] A "keep the old, union the new, cap the total" ratchet only retains anything when TAK > INDATA — if the fresh input alone fills the ceiling, eviction removes exactly what's missing from this round and the result is byte-identical to the fresh input. Shrink the input (or pay for a bigger ceiling); ENLARGING the input does not help — an entry that left the input is evicted at any depth. That wrong intuition was acted on twice in BIN-823, once by me and once by the closing reviewer, both defending it with aggregate-retention numbers (a deeper input is a more stable input, so retention rises and the no-op hides). Test the decisive case instead: force one id out of the input, assert whether it survives. Shipped 800-derive against a 1000-ceiling. (2026-08-08)
- [Workflow] A hold reason inherited from a PRIOR run's plan text outranked the deterministic router — 2026-08-06 withdrew BIN-759 and BIN-468 (finished, green, reviewed) for a "panel/critique owed" that `node docs/org/route.mjs` says does not exist: both return `tier:"skip"` at HEAD. Re-run the router on the batch's ACTUAL files at selection, every run; never inherit a tier from a plan, a ticket comment, or a previous withdrawal note, and print the router's raw output beside any hold that overrides it. Related: `docs/org/ownership-map.json` lists 149 EXACT paths (no globs), so a new file in an owned directory routes `skip` and `src/lib/mediaTypeDocId.ts` has no owner at all — "unmapped" and "trivial" are the same string. (BIN-787/788)
- [Workflow] Two lists decide who reviews a change and widening one never widens the other: the router (`docs/org/route.mjs`) ADVISES, `.claude/shared-plugin.json` → `reviewGates` BLOCKS. BIN-805 shipped "route.mjs is code" into the router in 24f6612; the gate's patterns still match nothing under `docs/` or `scripts/`, so a commit touching route.mjs alone reaches zero reviewers — the exact self-clearing hole the ticket was filed about. Change both in one commit and prove the blocking one with a staged-diff probe. Same shape as cd8c59e's `.github/` gap. When auditing a shipped review rule, check WHERE it runs. (BIN-830, 2026-08-09)
- [Testing] A script whose CLI runs at import cannot be tested — it eats the test runner's argv, blocks on stdin and `process.exit(1)`s, and the symptom is vitest hanging 15 minutes with NO output, not an error (BIN-802). Guard the CLI behind an entry-point check and wrap it in `main(argv)` first. Second wall in the same ticket: a test file outside the runner's `include` globs is silently never run by `npm test` while passing when invoked by hand — check the globs, add one additively, and confirm the new file's name appears in the full run's file list. (2026-08-09)
- [Workflow] Derive every per-ticket disposition at close-out from the TREE, never from the worker's self-report — 2026-08-12 reported "nothing was attempted" onto BIN-851 AND BIN-803 while five files sat STAGED (`.claude/shared-plugin.json`, `docs/role-responsibilities.md`, `docs/org/ownership-map.json`, `gen-ownership-map.mjs` +190 lines, new `ownership-gaps.json`) with a unique patch artifact beside them. A "never ran" claim against a non-empty `git status --porcelain` is a contradiction to fail on, not to transcribe; the tree wins and the false comment gets corrected in the same pass. BIN-707/708/713 backwards — and worse, because the next sprint re-picks the ticket and any clean-tree step deletes the only copy. (BIN-866)
- [Workflow] Ett ADR med `Status: Accepted` slår kommentarstråden — läs statusfältet FÖRE tråden. BIN-816 stod still i fem dagar för att tre sprintar i rad läste en äldre "väntar på Malin"-kommentar bredvid svaret, och den fjärde skrev en NY kommentar om att den väntade på en panel som redan gått. En tråd är append-only och osorterad efter sanning; `Status` finns just för att vara den enda platsen som svarar "är det avgjort?". (2026-08-13)
- [Testing] Ett tillstånd som uppstår och försvinner syns inte i slutläget — driv hold → observera → släpp med en styrbar promise. Att läsa context-värdet mitt i förloppet missar det (effekten ligger en render efter) och ett enda `act()` runt hela körningen batchar bort mellanläget. BIN-816:s "limbo-skärmen får inte visas under en normal radering" tog tre försök innan den bet. Samma runda: ett test som sätter förutsättningen fel når aldrig grenen det testar och är grönt av fel skäl — en markör satt före inloggning gör profilen null, så effekten återvänder innan spärren. (2026-08-13)
- [Design] En spärrhake som räknar i ANDEL låser sig när underlaget är litet — lägg ett absolut golv under den (`n > max(FLOOR, checked * FRACTION)`) och pröva det avgörande lilla fallet med bokstavliga tal. Ett tak på 50 kan aldrig lösa ut när användarbasen är under 50; 25 % vägrade 1 av 3 konton permanent, eftersom kandidatmängden är monoton och nämnaren bara växer med nya registreringar. Två avbrutna raderingar hade stängt av BIN-816:s sopning för gott. Golvets STORLEK var dessutom opinnad upp till 29 — alla testfall rörde sig med konstanten. (2026-08-13)
- [Workflow] En kapacitetskoll vid utdelning måste täcka VARJE tier över `skip` — BIN-744/776 skrev regeln bara för `full-panel`, så sprinten 2026-08-16 drog korrekt ut BIN-909 (`top`) och lät samtidigt BIN-908 och BIN-880/906 (`medium`) committas och pushas med den blinda rollkritiken oskuldad. "Bygg den, men parkera för påseende" är inget skydd: kritiken körs då aldrig av någon, den skjuts till en människa som inte är rollen, på kod som redan ligger på main. Batch-1:s skyldiga granskare var #25 — rollen som äger kvalitetsgrindarna — på en commit vars syfte var att vidga dem. Läs regeln där den UTLÖSER, inte bara där den skrevs. (BIN-917)
- [Testing] Ett `.mjs`-skript med CRLF kan inte ens PARSAS av vitest på Windows — Vites shebang-strippare är LF-only (`/^#!.*\n/`), så ssr-preludiet hamnar framför shebangen och rolldown dör på `Invalid Character !`. `npm test` hoppar då TYST över sviten lokalt medan CI på Linux är grön. Pröva hypotesen med en ORÖRD granne innan du misstänker din egen ändring; kör mot LF-normaliserade kopior (snapshot till scratchpad, återställ, verifiera med `git diff`). Samma runda: ett muteringsregex skrivet för LF rapporterade "MUTANT applied: false" mot CRLF-filen och korrumperade radsluten vid andra försöket. (BIN-891, 2026-08-16)
- [Workflow] Kanalen som följdbiljetter filas i kan vara FULL — `create_issue` svarade "You've exceeded the free issue limit" på sju följdbiljetter i post-sprinten 2026-08-16, bl.a. hålet där tre av elva valda biljetter aldrig delades ut. Ett fel från spårningssystemet är en leveransfråga, inte ett hinder att runda: skriv varje fynd som en fullständig kommentar på närmaste befintliga öppna ärende, märk varför det inte blev en egen biljett, upprepa HELA listan på ett ställe, och rapportera taket som något Malin måste åtgärda. Annars blir "kunde inte filas" och "hittade inget" samma sträng — BIN-707/708:s evaporationsklass. (2026-08-16)
- [Workflow] Ett tal eller ett "den enda X" i en kommentar är ett OKONTROLLERAT PÅSTÅENDE tills du kört något — typkontroll, tester och lintern läser inte prosa, så den enda kontrollen är att någon mäter för hand. BIN-905/918 tog 13 helhetsgranskningspass; 12 hittade något och NOLL var en defekt i koden. Alla tolv var meningar jag skrivit utan att köra kommandot: fel commit-tidpunkt (39 min, inte 92) inne i rättelseraderna som lagade felaktiga uppgifter, "fixturerna är de faktiska byten" om trimmade kopior, "sjunde ordet" om en lista med sex, "flaggan sätts på ett ställe" om tre, "loggens enda skrivare"/"enda anroparen" om en fil med två anropare som dessutom kringgås av motorn, och en plan som körde blind kritik från de två roller routern VÄLJER BORT. Två av de tolv satt inne i fixen för ett tidigare fynd. Kör kommandot före meningen; be granskaren behandla varje tal som misstänkt och redovisa vilka den räknade om. (2026-08-17)
- [Workflow] En kapacitetskoll som tittar pa BILJETTENS filuppsattning missar den fileuppsattning FIXEN kraver — BIN-766 routade `medium` pa sina egna filer, men #27:s bindande villkor drog in `firestore.rules` och en omkorning gav `top` med full panel. Kor om routern nar en kritik VIDGAR omfanget, inte bara vid urvalet; annars byggs en top-risk-andring under en enskild rollkritik. (2026-08-19)
- [Workflow] Granskarens RAPPORT om vad den last ar inte bevis — granskningsloggen ar. Fyra pa varandra foljande integrationsgranskningar pastod alla att de last samtliga sju filer och gick igenom pa sak; loggen visade tre, sex, sex och sju. Push-grinden kraver EN korning som tacker allt, sa tre nastan-kompletta korningar ar noll. Grep loggen sjalv pa `git rev-parse HEAD:<fil>`-shan innan du kor om, och begar ett kontrollerbart bevis (citera forsta raden i varje fil) i stallet for en sammanfattning. Samma klass som "kor kommandot sjalv nar en agent motsager dig". (BIN-766, 2026-08-19)
- [Workflow] Ett antal i en kommentar overlever inte tre granskningsrundor — samma mening om vilka moduler som skriver till en samling foll tre ganger i rad: "enda skribenten" (tre finns), "alla tre bygger id via mediaTypeDocId()" (en anropar den noll ganger), "exakt tva bygger nagonsin ett id" (en lasvag bygger ett med). Varje rattelse bar ett NYTT omatt tal. Avgransa pastaendet till det spärren faktiskt bryr sig om (har: id byggda for en SKRIVNING), rakna med ett kommando, och skriv processberattelsen i sprintplanen — inte i koden, dar den inte hor hemma enligt doc-taxonomin. (BIN-766/941, 2026-08-19)
- [Workflow] Ett radnummer i ett plandokument är falskt i samma commit som det ligger i — namnge funktionen, testet eller symbolen i stället, och skriv kommandot bredvid varje tal. BIN-954 rättade samma anropsställeslista TRE gånger under fyra granskningsvarv och hade fel tre gånger, eftersom varje ny kommentarsrad flyttade raderna igen. Samma runda: ANKARET kan vara fel även när talet är rätt räknat — `await setDoc(ref` gav åtta skrivvägar, men ett create-golv träffar varje `merge: true` (tio). (2026-08-20)
- [Design] En ny cache av "dokumentet finns" måste följa raderingen överallt den gamla gör det — och ett märke satt EFTER ett await skyddar bara den sekventiella kedjan, inte en samtidig systeroperation. Firestore lägger på väntande skrivningar optimistiskt, så "Ta bort" blir klickbar innan tillägget svarat; raderingens rensning träffar då en nyckel som ännu inte finns och tillägget skriver tillbaka den över ett raderat dokument. Bumpa en generationsräknare SYNKRONT före raderingens första await. Att märka FÖRE skrivningen är sämre, inte bättre. Bet BIN-954 två granskningsvarv i rad. (2026-08-20)
- [Workflow] Commit-grinden läser granskarens SISTA domrad, inte om fyndet är lagat — en `fail` blockerar tills SAMMA granskare kört om och slutat på pass, även när felet satt i ett plandokument som ingen `reviewGates`-pattern matchar. Kör om med `Read` på varje fil (ledgern bokför bara Read-verktyget) och EN körning som täcker allt. Samma commit: `review-coverage` kräver en STAGEAD `review`-rad i `docs/org/metrics/events.jsonl` för biljetten i ämnesraden — och `log_event.mjs review` utan argument skriver en tom rad som måste plockas bort för hand. (BIN-954, 2026-08-20)
- [Workflow] Ett felaktigt påstående i en kommentar eller ett beslutsprotokoll STRYKS, det formuleras inte om — en omskrivning bär ett nytt omätt påstående, och så blir ett fynd en kedja av rättelser (Synat natten till 2026-08-16: 11 rättelsecommitar på 2,5 timmar, `cd69009` införde ett nytt räkneord medan den tog bort ett). Rätta på plats bara när den sanna lydelsen är direkt läsbar ur koden och inte behöver räknas. Tre undantag: beslutsprotokoll ersätts av en daterad efterföljare, `*.knowledge.md` följer sin arkivkonvention, och regeln får ALDRIG radera protokollet över olöst arbete — ett blockerande fynd eller ett ouppfyllt acceptanskriterium stängs genom att koden lagas och granskaren kör om. (2026-08-21)
- [Workflow] En hållen patch FÖRFALLER, och biljetten som beskriver den åldras tyst — kör `git apply --check` vid URVALET varje körning, ärv aldrig ett "går rent mot HEAD" ur biljettexten. BIN-972 redovisade sex filer och noll fel 2026-08-23; samma kommando gav sex fel 2026-08-25, den första för att BIN-989 döpte om `.claude/hooks/map-freshness.mjs`. Sant när det skrevs, falskt två dagar senare, utan att någon rörde biljetten. Och när den inte längre går rent: läs patchen hunk för hunk mot trädet — mycket kan redan ha landat under andra id:n — bygg inte om den blint. (2026-08-25)
- [Workflow] En granskare kan läsa SIN EGEN definition i stället för filen den grindar och ändå avsluta på pass — `binge-security-reviewer` listade sex lästa filer, läste `binge-security-reviewer.md` och missade `binge-integration-reviewer.md`, en av bara två stageade filer dess mönster matchade. Commiten refuserades med filen namngiven. Ny variant av 2026-08-19: inte för FÅ filer, utan rätt antal och FEL fil. Härled granskarens skyldiga fillista ur `reviewGates` själv före commit och jämför med rapporten; räkna med ett omkörningsvarv i budgeten. (2026-08-25)
- [Testing] En trasig JSON i en DATAFIL ser ut som hundratals orelaterade testfel — ett `\.`-escape som blev ogiltigt i `.claude/shared-plugin.json` tog `docs/org/`-sviten från 967 gröna till 818 fällda, spridda över filer utan koppling till ändringen, eftersom `gate-symmetry.test.mjs` och `route.test.mjs` läser konfigen som indata. Kör HELA sviten efter varje redigering av en fil andra test läser som data, och verifiera parsningen separat med `node -e "require('./<fil>')"`. Symptomet pekar åt fel håll. (2026-08-25)
- [Workflow] Återställ ALDRIG en fil åt en syskonagent som muterar den — `firestore.rules` visade en diff tre gånger under avslutningen, en gång strukturellt trasig (en verifierares `String.replace` expanderade `$'`). Att städa hade landat mellan agentens för- och efterkontroll och gett ett falskt mutationsresultat; 2026-08-05:s kollision med rollerna ombytta. Håll din EGEN `git show HEAD:<fil> > scratchpad/<fil>.HEAD-<sha>` som återställningsväg (agentens scratchpad-kopia kan skrivas över med dess mutant) och verifiera med `git hash-object` före stage. (2026-08-25)
- [Workflow] Den enda strykning som håller skriver ingen mening alls — ersätt en inaktuell uppräkning med ett KOMMANDO som härleder den, inte med en mening om mängden. BIN-979 tog tre försök på samma sex rader: 2026-08-23 skrev ett falskt universellt påstående, mitt första försök 2026-08-25 skrev "en enda arvande fil" (routern namner tre) plus en hinkuppdelning som höll 15 av 18 och ett färskt räknefel i golvkommentaren, och först det tredje — som klistrar in `node -e` i stället — höll. Att undvika siffror räcker inte: "den enda X", "alla Y", "de under Z svarar W" är samma sak i ord. Testet är om ett kommando kan motsäga meningen. Stryk i samma redigering den `Corrected …`-notering vars subjekt försvinner. (2026-08-25)
- [Workflow] Ett tal vars sanning hänger på en OUTTALAD premiss går inte att härleda om — stryk det, skriv inte ett annat. Sprinten 2026-08-26 tog elva blockerande fynd; varje enskilt var ett falskt påstående i min egen prosa, noll var defekter i koden, och två satt inne i fixen för ett tidigare fynd. Avgörande fallet: "muteringen fäller fyra test" mot granskarens tre — båda rätt, för ett bart `watchedAt != null` fäller fyra och ett `seenDate(i) != null` tre, och ingendera muteringen namngavs. "Tre" hade shippat samma defekt med en annan siffra. Kontrasten en sådan mening behöver är oftast något mot ingenting.
- [Workflow] En muteringsprövande granskare kolliderar med en LÄSANDE, inte bara med en annan muterare — 2026-08-14:s "läsande får gå parallellt" gäller inte när motparten muterar. Att starta kodgranskaren medan integrationsgranskaren läste om samma bunt gav en fälld runda på två mekaniska grunder (levande mutant i trädet; ledgern pinnade mutantens bytes), noll fynd i koden. Kör muterande granskare en i taget, och integrationsgranskaren SIST mot ett städat träd — den är push-grinden och måste läsa exakt de bytes som går ut. (2026-08-26)
- [Testing] En grön bunt är inte en grön svit: en NY fil under en katalog ägarkartan listar fil för fil är en ägarkartshändelse. `src/lib/markedSeen.ts` var grön i varje per-bunt-körning och fällde två test i `docs/org/gen-ownership-map.test.mjs`, som läser ägarbaslinjen som indata. Ge filen en ÄGARE i `docs/role-responsibilities.md` och regenerera — aldrig `--update-gaps`, som gör hålet permanent (BIN-1013). Kör hela sviten före push. (2026-08-26)
- [Workflow] En restlucka mäts mot en KLOCKA — namnge vilken innan du beskriver vad som faller utanför, och läs meningen mot operatorn. BIN-909 mäter Auth-kontots egen ålder, som en radering av `users/{uid}` inte rör; jag skrev tre gånger att hålet var "raderas och återbesöks inom fem minuter" (fel klocka — ett konto som passerat tröskeln gatas hur snabbt ägaren än återvänder), och fjärde versionen behövde dessutom rätta gränsen, eftersom `>` är strikt och exakt fem minuter ligger på den OGATADE sidan. Gränstestet sa rätt sak hela tiden; meningen bredvid var fel. Fyra granskningsvarv, sex fynd, ETT i koden — två av de fem falska satt inne i rättelsen av ett tidigare fynd. (2026-08-27)
- [Testing] En flagga som sätts efter en rundtur skyddar inte fönstret FÖRE den — kontrollera att grindens flagga är sann tillräckligt tidigt, inte bara att den är sann. `onAuthStateChanged` sätter `uid` synkront och watchlist-lyssnaren startar på det, medan `pendingReconsent` är en `getDoc` bort; Firestores lokala cache levererar rader i glappet, så de självgående skrivarna skrev under ett uid vars profil saknades — precis det grinden byggdes mot. `deletionInProgress` har inte hålet (synkron localStorage-läsning). Fixen låg redan i filen: `profileLoading` sätts i samma synkrona block som `setUid`. (BIN-909, 2026-08-27)
