# /linear scan campaign — 2026-06-22

> Supersedes the prior night-scan inventory (passes 1–9, 2026-06-14, 34 tickets BIN-10..40 —
> that history lives in `.claude/linear-tracker.json` → `scanNight`).

Varied-type scan campaign over the **96 commits since the last deep scan** (`616fa05` → `5ad844e`),
covering an almost entirely new feature surface that landed unscanned: Cineasterna rental scraper,
OMDb/community ratings, collaborative lists + reactions + feed, availableNotify push, CSV import,
streaming offers (MOTN), dark mode + PWA, wiki bios, advisor lenses, franchise collections,
insikter dashboard, multi-select library.

Board went from **2 open** (BIN-66 ops-blocked, BIN-135 Low) → **31 open** (filed 29: BIN-143–171).

## CENSUS — 29 tickets filed, worst-first

**Verified defects (21)** — safe to hand to /sprint-execute:
- **High (3):** BIN-143 public community-rating poisoning via unbounded `watchlist.rating` [rules change];
  BIN-144 CSV import short-title wrong-match + unused IMDb id; BIN-153 bulk-delete deletes filtered-out
  titles after a search, no confirm (irreversible data loss).
- **Medium (12):** BIN-145 leaving-badge last-day off-by-one; BIN-146 Cineasterna checkpoint+empty-fetch;
  BIN-147 reactions spoiler gate UI-only; BIN-154 watched-date edit inflates rewatchCount; BIN-155 free
  providers double-render; BIN-156 insights rollup unbounded 4×/day scan; BIN-157 MOTN fetch no timeout;
  BIN-158 insikter histogram scale; BIN-161 filter chips scale; BIN-162 CSV import rating scale; BIN-171
  "Avmarkera alla" lastWatched desync.
- **Low (6):** BIN-148 community-rating drift; BIN-149 orphan list co-editor; BIN-150 CSV write batching;
  BIN-151 PWA PNG icons + dark themeColor; BIN-152 wiki host validation; BIN-159 collection add-all writes;
  BIN-160 settings toggle coupling.

**Anchored product ideas (8)** — need your call:
- BIN-163 library-wide weekly leaving-soon + new-on-your-services digest (strongest, on-brand)
- BIN-164 custom tags · BIN-165 TV-episode date editing · BIN-166 onboarding watchedAt
- BIN-167 runtime-lens dim-not-hide · BIN-168 series bulk Avbryt
- BIN-169 list cheapest-watch optimizer (speculative) · BIN-170 Binge Wrapped (speculative)

## SYSTEMIC THEME — rating-scale confusion
Ratings are **0.5–5 stars** at the write site, but four *consumers* re-derived the scale wrong:
BIN-158 (histogram assumes 1–10), BIN-161 (filter chips ≥6 unreachable), BIN-162 (CSV import writes 1–10
into the 0.5–5 field, also pollutes the public aggregate), and `CommunityRating` has to hand-`×2`.
Recommended root fix: a shared `RATING_SCALE` constant + `toOutOfTen()` helper so the family stops recurring.

## Tooling census — CLEAN
lint 0 err / 35 warn (unused vars, below bar) · typecheck clean · 905 tests pass ·
npm audit 2 moderate (postcss transitively via Next — breaking downgrade to fix, below bar, pre-existing).

## Passes & convergence
| Pass | Type | Agents | New verified defects |
|------|------|--------|----------------------|
| 1 | deep wave 1 (new code) | 6 | 10 (2 High) |
| 2 | deep wave 2 (remaining churn) | 4 | 8 (1 High) |
| 3 | creative + rating-scale sweep | 2 | 2 bugs (+8 ideas) |
| 4 | convergence-confirm (thinnest files) | 1 | 1 |

**Stop reason:** diminishing returns. Defect yield per pass 10→8→2→1; deep scan covered 100% of the
changed surface; hygiene dry; the systemic rating-scale theme swept to exhaustion (grep confirms no 5th
instance); creative's strongest anchored ideas extracted (remaining ideas increasingly speculative).
A 5th pass would re-scan already-covered code.
